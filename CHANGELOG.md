# voice-calendar — CHANGELOG（build log・最新が上）

## v8 — 年・月は「今日に最も近い」を選ぶ：未来固定ルールを捨てた（過去も一級市民の徹底） (2026-07-16)

**背景**
- 実発話FB第4回: **「6月の30日というと、6月30日にはならないですね」**。7/16 に発話 → **2027-06-30（11か月先）**になっていた。
- 併せて v7 の「時間にひっぱられる」は**ユーザー自身が「v6 で修正済み・キャッシュが残っていただけ」と確認**（切り分け完了）。

**設計判断（v0 の日付セマンティクスを確定）**
- 原因は「**過去なら来年へ倒す**」という未来固定ルール（一般的なカレンダーアプリの流儀）。これは v5 で確定した「**過去も一級市民**」（昨日の11時半…だった＝実績も声で入れる）と**正面から矛盾**していた: 昨日は過去に入るのに 6月30日 は来年に飛ぶ。
- → **「言っていない上位単位（年・月）は、今日に最も近いものを選ぶ」**に統一。非恣意的で一文で説明できる:
  - 「6月(の)30日」(7/16) → **2026-06-30**（-16日 ≪ 来年の +349日）
  - 「1月5日」→ **2027-01-05**（+173日 < 今年1/5の -192日）＝従来と同じ結果
  - 「20日」→ 今月20日（+4日）／ 7/28 に「5日」→ **来月5日**（+8日 < 今月5日の -23日）
  - 「2月末」→ 今年2月末（-138日 < +227日）
- 代替案「今年と仮定し、N日以上過去なら来年」も検討したが、**閾値 N が恣意的**なので採らなかった。「最も近い」は自然に半年が境界になる。
- **中間帯（半年前後）は本質的に曖昧**（「3月1日」＝4か月前 or 8か月先）。どのルールでも当たらない。→ **v1 の `guessed`（仮＝淡色/点線）表示の第一候補**として TODO に記録（FieldState に席は予約済み）。

**ハマったところ（テストが捕まえた新種のバグ）**
- 「2月30日」を N月N日 が正しく拒否した後、**素の「30日」ルールが拾って 7/30 を作っていた**＝ユーザーが言っていない日付の創作（背骨違反）。→ **blocked スパン**を導入: 無効な日付は消費しない（タイトルに残して見せる＝素通し）が、後続パターンの再解釈は禁止。存在しない日付（2月30日・4月31日）は素通し＋理由表示に。

**結果**
- テスト 76/76（parser 62 + schema 14）。preview E2E: 報告ケース「6月の30日」→ 2026-06-30 ✅・「6月30日 打ち上げだった」→ 2026-06-30 ✅・「1月5日」→ 2027 ✅（退行なし）・「2月30日」→ 素通し＋理由 ✅・console 0。

**教訓**
- **学び（過去も一級市民）を1箇所に適用しただけでは足りない**。v5 で相対日（昨日/一昨日）だけ直し、絶対日付（N月N日）の未来固定を残したため、同じ製品の中に2つの時間観が同居していた。**原則を変えたら、その原則が効くべき全パターンを洗う**。

## v7 — 「N月のN日」対応：月が黙って無視される事故を修正＋Pages キャッシュの教訓 (2026-07-16)

**背景**
- 実発話FB第3回（スクショ3枚）。報告は「日時だけの発話なのに前の時間にひっぱられる。来歴は日だけで正解」＝ v6 で直したはずの挙動。

**切り分け（重要な教訓）**
- ローカル v6 で同じ3連発話を再現 → **正しく時刻が掃除される**（cleared: startTime・🧹表記）。来歴に 🧹 が無い点も含め、症状は v5 の挙動と完全一致 → **iPhone が Pages の10分 CDN キャッシュで v5 を見ていた**と断定。v6 公開 15:56 に対しテストが 15:58〜だった。
- **教訓: FB を受けたらまず「フッタの BUILD 版」を確認してもらう**。Pages は `max-age=600`＝push 後10分は旧版が出る。急ぐならプライベートタブ。

**スクショから見つけた本物のバグ**
- タイトルが「**7月の**移動」「7月の**のの**移動」＝「N月**の**N日」の「の」未対応で、**月が黙って無視され素のN日として解釈**されていた。今回は 7月＝今月だったので日付は偶然一致。**「12月の5日」なら 2026-08-05 に化けていた**（5<16→来月ルール）。→ N月N日 の正規表現に `の?` を追加。事故ケース（12月の5日）をテストに固定。

**結果**
- テスト 71/71（parser 57 + schema 14）。preview E2E: FB の3連発話がタイトル残骸なし・3発話目で時刻掃除＋🧹 まで期待どおり・console 0。

## v6 — 発話＝言い直し：前回の音声欄を掃除（--:--）＋今月の末/N月の末/N後の今日 (2026-07-16)

**背景**
- 実発話FB第2回（来歴パネルのスクショ付き＝パネルが早速機能）。認識信頼度 95〜99% と高く、**外れはすべて解釈側**と切り分けできた。ユーザー指摘: **「時刻が読み取れなかった発話のあと、前回の時刻が残って混乱する。--:-- になる方が分かりやすい」**＝ v1 から観察メモに載せていた「連続発話の混在状態」が実地で問題化した。

**設計判断（v0 の発話セマンティクスを確定）**
- **「1発話 = 1つの予定の言い直し（再描画）」**: 発話が触れなかった欄のうち、**前回の音声が書いた欄（origin='voice'）は空に掃除**する。**人が手で入れた欄（origin='human'）と編集中ロックの欄は消さない**——音声は言い直しが安いが、手入力は労力なので保護（§8「編集中の経路を保護」の延長）。
- そのために origin（'voice'|'human'|null）を fieldState と別に持つ。掃除は `cleared` として通知し、来歴にも「🧹 前回の開始時刻を空に」と残す（黙って消さない）。
- 何も解釈できない発話（雑音）はフォームを触らない＝掃除もしない。
- 発話の断片で欄を「追加」するのは v1 の差分パッチの主戦場（変更なし）。

**パーサ追加（来歴のスクショから）**
- 「**今月の末**仕事」→ 素通しだった: 月末系に「の」を許容（今月の末/来月の末）。
- 「**N月の末**」「N月末」を追加（「9月末」は従来 内側の「月末」に部分一致して今月末に化けるバグ寸前だった＝先に専用パターンで拾う）。過去の月は来年。
- 「**1ヵ月後の今日**暇」→ 「の今日」を別日付と誤判定→曖昧素通しになっていた: N後 パターンが「の今日」まで消費。
- ガードテスト: 「週末」を月末と誤読しない。

**結果**
- テスト 67/67（parser 53 + schema 14）。preview E2E: ユーザーの混乱シナリオそのもの（11時半の発話→時刻なしの発話）で時刻が空に掃除・「今月の末」=7/31 が入る・手入力の場所は言い直し後も残存・🧹 が来歴に出る・console 0。

**教訓**
- **来歴パネルが初回から効いた**: 認識%が高いと分かったことで「解釈側の問題」と即断でき、スクショ1枚で3件の実データが取れた。計測（SPEC §10 の切り分け）は UI に埋めると回収コストがほぼゼロになる。

## v5 — 🎉 実発話FB第1回を反映：昨日/一昨日・「来週の◯曜」・N後（漢数字）＋来歴パネル (2026-07-16)

**背景**
- iPhone Safari（GitHub Pages）で初の実発話テスト。ユーザーの総評 **「いいかんじだとおもう。基本いける」**＝核（音声→フォーム→ノールック）の初検証が当たった。外れ3系統が実データとして得られた——まさに「素通しで実データを測る」（SPEC §12）の狙いどおり。

**実発話FBと修正**
1. **「昨日の11時半暇だった」→ 開始が明日になった**: パーサに過去日が無く、時刻だけ拾われ「過ぎた時刻→明日」ルールが発動。→ **昨日/一昨日（きのう/おととい）を追加**。ここで重要な発見: **ユーザーは予定だけでなく「実績」も声で入れる**（〜だった）。過去日は一級市民にした（今週◯曜が過去でも埋める・先週◯曜も追加）。ライフログ移植（SPEC §11）を先取りする強いシグナル。
2. **「来週の月曜日旅行」→ 日付は合ったが偶然**: 「来週**の**◯曜」の「の」に未対応＝素の「月曜」として解釈。木曜発話の月曜はどちらも 7/20 で一致しただけで、「来週の金曜」なら1週間ズレていた。→ 正規表現に `の?` を追加（テストは差が出る金曜で固定）。
3. **「一か月後旅行」→ 素通し**: 相対日と漢数字が無かった。→ **N日後/N週間後/Nか月後/N年後**（算用数字＋漢数字一〜九十九、か/ヶ/ヵ/カ/ケ/箇月対応）を追加。月末越えは月末に丸める（1/31の1か月後=2/28）。
4. **「時間が勝手に8時になる」（来週の月曜日旅行）→ 未再現**: 現パーサはこのテキストから 8:00 を生成し得ない（数字が無いと時刻は付かない）。**認識された生テキストが記憶と違った可能性が高い**が、来歴が消える設計だったため特定不能。→ **来歴パネルを追加**（下記）。

**設計判断**
- **来歴パネル**: 発話ごとに「生テキスト＋認識信頼度＋解釈結果＋素通し理由」を localStorage に30件記録し、畳みの中に表示。SPEC §5-①（来歴の保存）の見える化で、**「認識が悪いのか、解釈が悪いのか」（SPEC §10 の切り分け）をユーザー自身が報告できる**ようになる。端末内のみ・外部送信なし（ローカル完結）。
- 相対日は素のN日より先に拾う（「3日後」が「3日」に食われない、重なりは候補収集の overlap 検査で排除）。

**教訓**
- **実発話は「予定を入れる」と限らない**。過去形（〜だった）が初回テストから出た。入力エンジンはカレンダー（未来）でなく時間記録（過去も未来も）として設計するのが正しい——ライフログ移植の設計を強化する学び。
- 「たまたま合う」ケース（木曜の「来週の月曜」）はユーザー報告では「完璧」に見える。**ズレが出る組み合わせをテストに固定する**（来週の金曜）ことが決め打ちパーサの守り方。

**結果**
- テスト 56/56（parser 46 + schema 10）。preview E2E: FB4ケース全部が期待どおり＋来歴4件記録・描画 ✅・console 0。

**残課題 / 次の方向**
- 「8時」の再現待ち: 次に起きたら来歴パネルの 🗣 行（生テキスト）で認識/解釈を切り分ける。
- 相対時刻（30分後/2時間後）は未対応（実データが出てから）。

## v4 — root に本体→sync-web で www/ 生成、GitHub Pages 配信＝iPhone Safari で実発話検証へ (2026-07-16)

**背景**
- PC に音声入力装置が無い＝実発話の検証（v0 の次の一手）が PC ではできない。リポは public 済み → あの日と同じ **GitHub Pages で iPhone Safari から触る**道をユーザー GO で実施。iOS Safari は webkitSpeechRecognition 対応＝**native を書く前に本命端末でノールック完走率が測れる**。

**設計判断**
- Pages はブランチ直下 or /docs しか配信できない → **web 本体を root へ移動**（index.html / engine/ / input/ / adapters/）し、Capacitor の webDir（www/）は `scripts/sync-web.mjs` が生成する構成に（photo-memory-spike と同一の運用。sync-web.mjs はほぼ流用）。www/ は gitignore＝生成物。
- script の参照は元から相対パス（`engine/parser.js`）なので、Pages のサブパス（/voice-calendar/）でも root でもそのまま動く。移動は `git mv`（履歴保持）。
- npm scripts をあの日に合わせ `sync:web` / `cap:copy` / `cap:sync`（cap の前に sync-web が必ず走る）に。`serve` は root 配信に変更。

**結果 / 観察**
- sync-web で www/ 生成 ✅・テスト 45/45 ✅・root 配信のプレビューで v4 BUILD 稼働＋発話シミュレート反映 ✅。
- Claude の Browser ペインはマイクをブロックする（通知で確認）＝ PC 側で実発話が試せないことの裏取り。iPhone 路線が正解。

**残課題 / 次の方向**
- iPhone Safari での実発話10件（素通し率・外し方の実データ）→ パーサ補強。
- iOS Safari の webkitSpeechRecognition は Siri/音声認識の OS 設定に依存する可能性 → 実機で確認（ダメでもテキスト欄で体験は試せる）。

## v3 — 🔴 欄ロックの実バグ修正：ストアと画面がズレて「画面と違う値が保存される」を潰した (2026-07-16)

**背景**
- v1 の preview 検証中、ロック時の toast だけが出ないことに気づいた。「見た目の些細な差」に見えたが、憶測せず実測したら**核心（SPEC §8・背骨①）の correctness バグ**だった。

**症状 → 原因 → 対処**
- **症状**: `document.activeElement` は `f-title` なのに `store.getFieldState('title')` は `'confirmed'`、`applyVoicePatch` の `written` に `title` が入り**ストアが上書きされていた**。画面が「歯医者」のままに見えたのは `render()` が activeElement をスキップしていたからで、**ストアには「定例」が入っていた＝そのまま保存すれば画面と違う値が書かれる**。
- **原因**: ロックが **focus/blur イベント基準の Set**、描画スキップが **activeElement 基準**＝**同じ「編集中か」を二重管理**していた。イベントが発火しない経路（プログラム的 focus・背面タブ、実機では webview の keyboard/background 復帰など）で両者がズレる。ズレた瞬間「音声は書く・画面は隠す」＝**サイレントに嘘の保存**。これは背骨①「フォームが単一の真実」の直接の違反。
- **対処**: **ロックの正を「宿主に聞く述語」に一本化**（`store.setLockSource(fn)`）。UI は `(f) => inputEl(f) === document.activeElement` を注入＝**描画スキップと同じ根拠から導出**され、構造的にズレ得ない。`render()` / `applyVoicePatch` / `getFieldState` すべて `store.isLocked()` 経由に統一。✋マークの CSS も `render()` から同期（見た目も実挙動と一致）。明示 `lock()/unlock()` は DOM を持たない宿主・テスト用に温存し、判定は両者の OR。
- engine は DOM 非依存のまま（述語は宿主が注入する関数＝中立）。移植性は損なわれていない。

**設計判断**
- 「イベントで状態を作る」より「**状態を現実から導出する**」。ロックの現実＝どこにフォーカスがあるか。導出なら同期漏れが原理的に起きない。

**教訓**
- **核心機能にテストが無かったから見逃した**。パーサ（35本）はテストで固められていたのに、`schema.js`（＝背骨①③と §8 が宿る場所）は0本だった。→ `tests/schema.test.js` 10本を追加し、この回帰も固定（「lockSource が locked と言う欄は getFieldState も locked」）。**テストの厚みは「難しさ」でなく「壊れたときの怖さ」で決める**。
- **UI の些細な違和感（toast が出ない）が correctness バグの sentinel だった**。E2E で "値が期待どおり" だけを見ていたら見逃していた（画面の値は正しく見えていた）。**store と DOM の一致そのものをアサートする**のが正しい検証。

**結果**
- 修正後: `isLocked: true` / `skipped: ['title']` / **`storeVsDom: true`** / toast 表示 / ✋マーク表示、blur 後は全部解除されて音声が書ける。テスト 45/45（parser 35 + schema 10）。console エラー 0。

## v1 — リポ bootstrap＋一本道(web)貫通：転写→確定パーサ→フォーム→欄ロック→ics 保存 (2026-07-16)

**背景**
- 要件定義（SPEC.md v0）が設計対話で完成 → 新規リポとして立ち上げ。付記の指示どおり三層（CLAUDE/TODO/CHANGELOG）に展開し、最初の切り口=「録音→確定パーサ→フォーム→保存」の一本道を、Windows で検証できる web 層で先に貫通させた。
- iOS native（EventKit / SFSpeechRecognizer / App Intents）は Phase 2-3。エンジンを先に固めるのは SPEC §6「中立スキーマは最初にやる（後付けは高い）」のため。

**設計判断**
- **層をファイル境界にした**（engine / input / adapters / UI）。あの日は単一 index.html だが、このプロダクトは「入力エンジンが資産・宿主非依存」が背骨なので、境界を物理的に引いた。バンドラ無しは同流儀（script 直読み + window.VC* + Node require 両対応）。
- **start/end を date/time 部分フィールドに分割**（SPEC §6 の `start: Date|null` の v0 具体化）。「明日 歯医者」＝日付だけ確定・時刻は空、を創作なしに表現するため。Date への実体化と保存時既定値（時刻なし→終日 / 終了なし→+1h / 無題→「予定」+warning）はアダプタに集約＝解釈層は絶対に埋めない。
- **曖昧の素通しを「見える」形にした**: 埋めなかった断片はタイトルに残す（「明日3時」→日付だけ入り「3時」がタイトルに残る）＋ notes に理由を表示。無言で捨てない・勝手に確定もしない、の中間。
- **決め打ちルールはテストが仕様**（tests/parser.test.js に35本固定）: 素の曜日=直近未来 / 「今週X曜」が過去なら素通し / 修飾なし1〜6時は曖昧→素通し / 時刻だけなら未来側（今日 or 明日）/ 「22時から2時」は日またぎ、など。
- **web の保存アダプタ = .ics ダウンロード**にした。stub より一段実物（OS のカレンダーが開ける）で、アダプタ差し替え（ics ↔ EventKit）の境界が最初から本物になる。
- **ロックは入力粒度**（startDate と startTime は別ロック）。「場所を打っている間も音声は日時に書ける」（SPEC §8）をフォームの実粒度で成立させるため。

**ハマったところ**
- preview_start がホームの `.claude/launch.json` を読む（リポ内の launch.json ではない）→ ホーム側に voice-calendar エントリ（port 5275、5273/5274/8123 と衝突回避）を追加して解決。
- **「プレビューできない」の真因＝dev サーバがターン間で落ちていた**（`preview_list` が空・python プロセスもゼロ・ポート未 listen →ブラウザは接続拒否）。ページ側は無実で、再起動＋HTTP 200＋全レイヤ配信＋JS で実測（全要素が実サイズ・4層ロード済み・`micAvailable: true`）を確認。**確実に触るには自分のターミナルで `npm run serve`**。
- Browser ペインの screenshot は **どのタブ（file:// の別ページ含む）でも timeout する環境**＝ツール側の不調でページのせいではない、と切り分け済み。検証は read_page + javascript_tool で代替する。

**結果 / 観察**
- パーサ単体テスト **35/35 ✅**。preview E2E ✅: 「明日15時に歯医者」→ タイトル/日付/時刻反映、**タイトル編集中に発話→タイトル保護＋日時は更新＋「✋そのまま」toast**、任意欄に音声が書いたら畳みが自動で開く、曖昧「明日3時」→時刻素通し＋理由表示、materialize→ics 生成正常。
- **発見: 連続発話の混在状態**。保存せず2回話すと前の発話の end が残る（パッチモデルの帰結）。実害は実使用で観察（TODO 観察メモ）。

**教訓**
- 「曖昧は素通し」を UI まで通すと、**素通しの痕跡がそのままユーザーへの説明になる**（タイトルに残る断片 + notes）。仮置き UI (v1) を作る前でも体験が破綻しない。

**残課題 / 次の方向**
- 実発話10件の手触り検証（マイク・自分の Edge/Chrome で `npm run serve`）→ 素通し率と外し方の実データ → パーサ補強（漢数字・「夕方」単独など）。
- Phase 2: cap add ios → speech-recognition / calendar-events ローカルプラグイン（iOS17 の書き込み専用カレンダー権限を検証）。
- **iPhone Safari 検証の道（判断待ち）**: リポ public 化により GitHub Pages が可能に。iOS Safari は webkitSpeechRecognition 対応＝native 前に本命端末でノールック完走率が測れる。ただし Pages はブランチ直下 or /docs しか配信できず、現構成は webDir=www に本体がある → あの日と同じ「root に本体 → sync-web.mjs で www/ 生成」への組み替えが要る。公開は外向き操作なので GO 待ち。

**リポ**
- https://github.com/yutsutke/voice-calendar （public・main・初回 push 済み）。git identity はグローバル未設定のため repo-local に設定（yutsutke / yutsutke@yahoo.co.jp、既存リポと同じ）。`git init` の master → main に rename。
