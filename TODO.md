# voice-calendar — TODO

> **これは何か**: 「明日15時に歯医者」と話すとフォームが埋まり、画面を見ずに保存まで完走できる（ノールック）入力補助カレンダー。音声と手は**同じ一つのフォーム**を同時に編集する。設計の正は [SPEC.md](SPEC.md)。
> 資産は「アプリ」ではなく「入力エンジン」＝後でライフログ `plans` へ移植する。

---

## 🔒 スコープの境界（最初に必ず読む）

- **v0 で作るのは SPEC §3 の9項目だけ**（道1・確定入力・2経路＋欄ロック・EventKit 保存・iOS）。
- **今は作らない**（→ 末尾リスト）: 道2 / 仮置きUI / 既存予定の読み取り / 音声差分パッチ / LLM / Android。
- 迷ったら SPEC §2「背骨」6点に照らす。

---

## 現在地

> **🎉 セッション1（2026-07-16）＝bootstrap → 一本道(web)貫通 → GitHub Pages 公開 → iPhone 実発話テスト第1回 →「基本いける」**
> v1-v3: エンジン＋フォーム＋欄ロック（v3 で core バグ修正）。v4: root 本体化＋ Pages 配信。**v5: 実発話FB第1回を反映**——昨日/一昨日（**過去の実績も声で入れる用途が判明**）・「来週**の**◯曜」・N日後/N週間後/Nか月後（漢数字対応）・**来歴パネル**（生テキスト＋認識信頼度＋解釈結果を30件記録＝認識と解釈の切り分け用）。
> **テスト 56/56 ✅**（parser 46 + schema 10）・preview E2E ✅・console 0。リポ = https://github.com/yutsutke/voice-calendar （public・Pages 配信中）。
>
> **👀 未再現 watch: 「来週の月曜日旅行」で時間が勝手に8時になった件**——現パーサはこのテキストから 8:00 を作れない＝認識テキストが違った可能性が高い。**次に起きたら「来歴」パネルを開いて 🗣 行をそのまま報告してもらう**（そのために来歴を作った）。※ v6 の「発話=言い直し」で前回の残り時刻は消えるようになったため、正体が「前回の残り」だったなら再発しない。
>
> **🔧 v6（実発話FB第2回・スクショの来歴パネルより）**: ①ユーザー指摘「時刻が読めなかった発話で前回の時刻が残って混乱」→ **発話=言い直しセマンティクス**（前回の音声欄は --:-- に掃除・手入力とロック欄は保護・🧹 を来歴に記録）②「今月の末」「N月の末」「1ヵ月後の今日」を追加。**テスト 67/67 ✅**（parser 53 + schema 14）。認識信頼度 95-99% ＝ 外れは解釈側と切り分け成功（来歴パネルが初回から機能）。
>
> **🔧 v7（実発話FB第3回）**: 「時間にひっぱられる」再報告 → ローカル v6 で再現せず・症状は v5 と一致＝**Pages の10分キャッシュで旧版を見ていた**（ユーザーも確認済み。📌 FB を受けたらまずフッタの BUILD 版を確認してもらうこと）。スクショから本物のバグ発見: **「N月のN日」の「の」未対応で月が黙って無視**（「12月の5日」→8/5 に化ける事故を修正・テスト固定）。
>
> **🔧 v8（実発話FB第4回・重要な原則変更）**: 「6月の30日が6月30日にならない」→ **「過去なら来年」の未来固定ルールを廃止**し、**「言っていない年・月は今日に最も近いものを選ぶ」**に統一（過去も一級市民＝v5 の学びを絶対日付にも徹底）。存在しない日付（2月30日）は blocked スパンで素通し＋理由表示（素のN日が拾って 7/30 を創作するバグをテストが発見）。
>
> **🔧 v9（実発話FB第5回）**: 「2027年11月5日→2026年になる」→ **年の語彙が丸ごと無かった**（2027年/来年/去年すべて無視され、v8 の推測が明示指定を上書き）。**「言った年には必ず従う／推測は言っていない時だけ」**を確立。絶対年＋相対年（今年/来年/再来年/去年/昨年/一昨年）を N月N日・N月末 に配線。**テスト 86/86 ✅**（parser 72 + schema 14）。
> 💡 **副産物**: v8 の既知の限界だった「中間帯の曖昧（3月1日=4か月前 or 8か月先）」は、**ユーザーが「来年の3月1日」と言えば自分で解消できる**ようになった → v1 の仮置き UI は作らずに済むかもしれない（下の観察メモ参照）。
>
> **🔴 v10（実発話FB第6回・自分で作った罠）**: v9 公開後も実機で 2026-11-05 のまま → オリジンの parser.js を直接叩いて裏取り（v9 コードあり・Age:0）＋デプロイ版をブラウザ実行（2027-11-05 が出る）→ **実機に古い parser.js だけが残っていた**と確定。真因＝**BUILD は index.html にしか無いのにロジックは別ファイル＝別キャッシュ**＝「フッタは v9・解釈は v8」が成立し、**「フッタで版を確認して」という案内自体が機能していなかった**。→ script に `?v=N` を付け、**tests/version.test.js で BUILD との一致を機械的に強制**（わざとズラして落ちることも確認）。**テスト 92/92 ✅**（parser 72 + schema 14 + version 6）。
>
> **🔧 v11（Phase 2 着手）**: 入力エンジンが安定したので iOS 足回りへ。`cap add ios`＋**speech-recognition**（SFSpeechRecognizer・on-device 優先・無音自動停止）＋**calendar-events**（EventKit・iOS17 書き込み専用アクセス・openSettings）＋転写の native 分岐＋Info.plist 権限＋codemagic.yaml。web 無退行確認済み。**Swift は未コンパイル＝初回 Codemagic ビルドが検証**。
> ⚠️ ハマり: Windows の `cap sync ios` は CapApp-SPM/Package.swift に**バックスラッシュのパス**を書く（Mac でコンパイル不能）→ 手でスラッシュに直した。CI は macOS で再 sync するので以後は自動で正しくなる。
>
> **🎉 v12（2026-07-16 夜）＝初回 Codemagic ビルド一発成功 → TestFlight 1.0 配信・内部テスター登録済み。** Swift プラグイン2本（SFSpeech/EventKit）の初コンパイル通過＝native 足回りが実在コードとして成立。あの日の資産（ASC キー MadeleineASC・Team 25TM5C27YT・codemagic.yaml）がそのまま効いた。輸出コンプライアンスは Info.plist で恒久回答（v12）。
>
> **🔴 v13（実機FB: TestFlight 1.0 で全ボタン無反応）＝2つのバグを修正、要リビルド。**
> ①真因: **`Capacitor.registerPlugin` は native に存在しない**（注入されるのは `Capacitor.Plugins.<jsName>`。registerPlugin は @capacitor/core＝バンドラ前提の API）→ TypeError。**あの日の index.html は最初から `Plugins.X` 優先で書いてあった**のに、私は app-shortcuts の doc コメントだけ真似た（v10 に続く2度目の「前提を確認せず作法だけ移植」）。
> ②設計欠陥: その例外がインラインスクリプトを止め、**音声と無関係な保存・クリアまで死んだ**（背骨①違反）→ 転写層は throw しない・テキスト送信は転写層に依存しない形へ。
> ＋ **画面に「診断」パネル**（環境/エンジン/native失敗理由/層の読込/初期化完走/全エラー・head で自己完結＝本体が全滅しても出る）。**テスト 98/98 ✅**（transcriber 6本を新規追加）。
>
> **🎉 v13 実機確認（22:20）＝復活**: 診断 native/sfspeech・全層✅・初期化✅・**認識97%で「明後日11時美術館」「明日10時美術館」が声で入った**（月日+時刻+要件が揃えば一本道が動く）。
> **🔴 残る症状（実機FB第7回）**: 「7月の」「2026年」「昨日」入りが入らない。**同じ文字列をテキスト欄から送ると通る**（パーサ単体 9ケース全通過を確認済み）＝見えているテキストと実際の文字列が違うか、途中で静かに死んでいる。
>
> **🔧 v14 ＝どちらの仮説でも次で判定できる形にした**:
> ①不可視文字の除去（iOS 音声認識が数字の前後に混ぜる双方向分離子 U+2066-2069 等）を normalize() に追加＋テスト5本
> ②onUtterance を try/catch 化＝**解釈が失敗しても必ず来歴に🛑付きで残る**（診断・toast にも出す）
> ※開発中、自分の出力にも不可視文字が混入して Edit が壊れた＝ソース中の不可視文字は必ず \u エスケープで書くこと
>
> ~~▶▶ v14 ビルド → 判定表で切り分け~~ → **実機FB第8回で「来歴に出ない」枝が確定**（マイク下に文字＝interim は届く・final が一度も来ない・テキストは正常）。
>
> **🔴 v15 ＝真因への対処**: `requiresOnDeviceRecognition=true` のオンデバイス認識は **endAudio しても isFinal を返さないことがある**（iOS の既知の癖）。v13 で短い発話だけ成功したのは認識器が自力確定できたから＝「7月の/2026年/昨日が入らない」は**内容ではなく長さ**だった（内容相関に見える機構バグ）。
> → **補完確定の保険**（endAudio 後2秒で final が来なければ最後の途中結果で確定・来歴に「補完確定」印）＋ **Swift 内部イベントを診断に可視化**（🎙 開始/無音タイマー/isFinal 到着/タイムアウト/エラー番号＝Mac なし開発の目）。**テスト 103/103 ✅**。
>
> ~~▶▶ v15 ビルド → 「isFinal が来ない」保険~~ → **実機の診断ログで仮説が外れたと判明**: 🎙「isFinal 到着」は出ていたのに来歴が空。
>
> **🔴 v16 ＝真犯人を特定・修正**: `deliverFinal` の `if !text.isEmpty { notifyListeners("final") }` ＝**空の確定を黙って握り潰していた**（自分で silent drop を仕込んでいた）。
> **決定的証拠（ユーザー観察）**: 「**手動で録音を切ると入る／自動で切れるのを待つと入らない**」＝ Swift は同一経路なので**差はタイミングだけ** → 無音1.8秒の間に認識器が内部で発話を終え、endAudio が**空の確定**を返していた。**ノールックは自動停止が前提なので v0 の核を直撃**していた（手動では動くので「音声は動いている」と誤認しかけた）。
> → 空なら**最後の途中結果で補完**（fallback印）・両方空の時だけエラー表示＝**絶対に黙って捨てない**。診断に `確定 len=N partial=M` を出す。v15 で自分が足した `audioEngine.stop()` も撤回。**テスト 103/103 ✅**。
>
> **🎉🎉 v16 実機成功（2026-07-17 朝・実発話FB第10回）＝v0 一本道が実機で成立**: 「自動でも入りました。タイトルと開始時間も入りますし、●時から●時までというので、開始と終了まで入りました」＝**ノールック経路（自動停止）で音声→フォームが動いた**。SPEC §0 の核が実機で成立した日。
>
> **🔧 v17（同FBの新要求）＝欄指定発話**: 「終了 22時」と言うと開始に入ってしまう→**欄名で始まる発話はその欄だけの差分**（掃除しない・SPEC §0「声か手で足す」の声版）。対応キー: タイトル/件名・場所・メモ・開始・終了。誤爆ガード2枚（欄名で始まる時だけ・値が解釈できなければ通常解釈へ）。来歴に 🎯 印。**テスト 119/119 ✅**。
>
> **🔧 v18（実発話FB第11回）**: v17 の欄指定は実機で「よさそう」＋**言い直し仕様（欄名なし発話＝新規）は意図どおり受け入れられた**（「それはそれで使いやすそう」）。新要求「意図せず新規になるので履歴からワンタップで戻したい」→ **来歴の各行に ↩「この発話の前に戻す」**（状態スナップショット方式＝発話の再解釈はしない・now がズレるため）。fieldState/origin も戻す・フォーカスを外してから復元（v3 の轍を回避）・多段可。**テスト 123/123 ✅**。
>
> **🔧 v19（実発話FB第12回）＝詳細設定**: 「言い直し（新規扱い）と手入力保護を選べるように＋他にあれば」→ **実機FBで実際に迷いが出た決定だけ6個**（新規扱い／手入力保護／欄ロック／任意欄の自動オープン／終了の既定長さ／無音の長さ）。**既定は全て従来挙動**（テストで固定）。engine は `setPolicySource` で述語注入＝中立のまま。各設定に `why` をコードに残す運用（設定は足すより消す方が難しい）。**テスト 137/137 ✅**。
>
> **🔧 v20-v22（実機FB第13-15回・詳細は CHANGELOG）**: v20 ↩ を「**行に見えている状態に戻す**」へ修正（表示と操作結果の食い違いを解消）／v21 来歴に扱いモードのバッジ（🆕新規・🎯欄指定）／v22 「場所は一度目だけミスる」への対処（`load()` でカテゴリ前倒し＋`contextualStrings` で欄名を認識器に教える＋診断に `起動Nms`）＋**「1発話1欄」を設計として確定**（複数欄の分割は「場所 **メモ**リアルホール」が壊れる実測により却下）。
>
> **🔧 v23（保存の足回り・実機FBの前に先回り）**: 問い「保存 → 本物のカレンダーに入るか。**これはもう実装したの？**」← **CLAUDE.md の `eventkit(iOS,未実装)` が嘘だった**（v11 実装・v12 コンパイル通過・**実機に載っている**。走らせたことが無いだけ）→ 修正。確認中に**権限拒否からの復帰導線が無い**穴を発見（Swift の `openSettings` は v11 から**誰も呼んでいないデッドコード**＝初回ダイアログで「許可しない」を押すと検証がその場で詰む）→ バナー＋「設定を開く」を配線。＋**保存先カレンダーを見る/選ぶ**（`EKCalendarChooser` は**書込専用アクセスのまま動く**と Apple ドキュメントで確認＝「追加のみ」の軽い権限を維持できる／`defaultCalendarForNewEvents` も write-only で動くと確認＝**現実装は実機で詰まない**）。**テスト 155/155**（**calendar.test.js を新規16本**＝Swift は Windows でコンパイルできない＝JS ⇄ Swift の契約は実機まで誰も検証しないため）。
>
> **🎉🎉 v23 実機成功（2026-07-17 昼・実機FB第16回）＝「iOS ネイティブカレンダーに書き込みできました」＝ v0 の一本道（声 → フォーム → 本物のカレンダー）が実機で完走**。v16 で「声→フォーム」、今日「フォーム→カレンダー」＝ SPEC §0 の核が端から端まで繋がった。
>
> **🔧 v24（同FBの新要求2つ）**: ①「**アプリを開いた瞬間に、すぐに録音できるようにしたい**」→ 起動/復帰（visibilitychange）で自動録音。**native のみ**（web はブラウザの権限モデル上タップ必須のまま）・**白紙の時だけ**（書きかけがある時に環境音を拾うと「発話=言い直し」(v6) が下書きを消し得る）・無音6秒打ち切り (v11) が自動停止を保証。設定にはしない（v19 の基準: 実FBで迷いが出てから）。**Phase 3（Siri 起動→即録音）の前提が先に完成**＝Siri は「開く」まで、これが「即話せる」。実機の診断に 🎙「自動録音（起動/復帰）」が出る。②「**Google カレンダーにも書き込みたい**」→ **コード変更不要＝v23 の「保存先を選ぶ」がそのまま道**（iOS 設定でカレンダーのアカウントに Google を追加 → アプリの「変更」で選ぶ。アプリは Google と直接通信しない＝SPEC §1-6）。設定のヒント文に手順を追記のみ。**テスト 155/155**。
>
> **🔴 v25（実機FB第17回）**: 「選んだカレンダーになっているのに iOS のカレンダーに保存されちゃう」＝ write-only で `calendar(withIdentifier:)` が効かない。真因＝チューザーが現物の EKCalendar を渡すのに識別子だけ保存して毎回再解決に賭けていた → 現物をプロセス内で保持。**→ v26 でこの機能ごと撤去（下記）**。
>
> **🎉 v26 の前提＝実機FB第18回「デフォルトカレンダーに書き込み成功！！！」**＝ OS 既定カレンダーへの保存が実機で成立。**Google も iOS 設定でアカウント追加 → デフォルトカレンダーに指定すれば入る**（アプリは Google と直接通信しない＝SPEC §1-6）。
>
> **🔧 v26（撤去）＝保存先の選択を消して OS の既定カレンダー1本に**: 実機で「アプリで Google を選んでも純正に入る」＝ v25 の現物保持でも**再起動のたび選び直し**が要る構造そのものが実用にならないと確定 → ユーザー判断「**保存先の撤去、OS のデフォルトカレンダーのみに保存**」。`EKCalendarChooser` / `chooseCalendar` / 設定 `targetCalendarId` / `import EventKitUI` を全削除。**「今どこへ入るか」の表示は残した**（設定ではなく事実＝「保存できたのに見つからない」を防ぐ）＋ OS 設定への案内文。**復活防止をテストで縛った**（実機2往復で買った知見＝忘れて作り直すのが最大の損失）。**テスト 154/154**。
>
> **🔧 v27（実機FB第19回・帰宅後）＝メモとしても使う**: 「**メモアプリとしても使えそうという感触**」＋要求2つ。①「時刻なしは今の時間で書き出して」→ **解釈が2通りあったので聞いた**（日付だけの「明日 歯医者」まで今の時刻にすると 22:45 を創作＝SPEC §7 違反）→ ユーザー判断「**日時を何も言わない時だけ今**」。v26 までは「開始が未入力」で**保存すらできなかった**＝メモ用途が通るようになった。**空フォームのガードを新設**（外した problems が実は空予定の防波堤だった）。②「今/現在で今の日時」→ **素で拾うと「今井さんと会議」が「井さんと会議」に化ける**（v22「場所 メモリアルホール」と同じ罠）→ **後ろが区切りの「今」＋完結した語（現在/たった今/ただいま/今すぐ/今から）だけ**。拾えない「今牛乳買う」は①が救う＝**2つの仕組みが補い合う**。**テスト 173/173**（誤爆ガードを固定）。
>
> **🔧 v28（実機FB第20回）＝話し終わったら自動保存（設定・既定オフ）**: 「記録後、保存を自動にできる設定」＝ノールックの究極形。条件は **SPEC §2-4「曖昧さゼロで黙って1個に確定できる時だけ」をそのまま**（設定オン＋変わった＋欄指定でない＋notes 空＋**日時を明示**）。🔴 **E2E で穴を発見**: 最初の条件だと「**えーっと**」が自動保存された＝パーサは意味を判定しない（LLM なし）＋v27「日時なし→今」＋v24「起動＝即録音」の**3つの交点**で「開くだけで環境音が予定になる」（保存は不可逆＝読めない＝消せない）→ **日時を明示した発話だけ**に限定して解決。**v27 の「今」の語彙が意図の表明として効いた**（「今 牛乳買う」は自動保存・「牛乳を買う」はフォームに残る）。**テスト 173/173**。
>
> **🎉🎉 実機FB第21回（v26-v28 ビルド後）＝日常投入レベルに到達**:
> - **v24（開いた瞬間に録音）「実機で、さわったら、完璧」**
> - **v27「現在で入力 今の時間がちゃんとでる」「日時なしでタイトルのみでカレンダーに保存 現在の時間が保存してくれた」「感動 日常で使えそう」**＝メモ用途が実機で成立
> - Siri:「ヘイシリ ボイスカレンダー開いて」は以前から通る（表示名 VoiceCalendar への音の照合）。一度開けない事象（電池残量少・再ビルドで復帰）＝ Siri 側の照合ゆらぎの可能性
> - 新要求: **「ヘイシリ ボイカレ開いてで開けるようにしたい」**（短い呼び名）
>
> **🔧 v29 ＝ Siri に別名を登録（Info.plist の INAlternativeAppNames のみ・Swift 変更なし）**: 「ボイカレ」＋カタカナ正式名「ボイスカレンダー」を明示登録（今までは英字表示名への音の照合頼み＝明示で確実にする）。発音ヒントはひらがな。iOS 16.4+（未満では無視されるだけ＝壊れない）。
> 💡 **これで道1（Phase 3）が App Intent 無しで完成する**: 「Hey Siri ボイカレ開いて」（v29）→ 開く → **自動録音**（v24）→ 話す → **自動保存**（v28・設定オン時）＝ SPEC §4 の道1が Swift の追加コードゼロで通った。計画していた App Intent / App Shortcuts / UserDefaults バッファは**動詞フレーズ（「ボイカレで予定入れて」等）が欲しくなった時だけ**の道具に降格。
>
> **🎨 v30（一般リリースへ着手）＝アプリアイコン**: 実機FB第21回「日常で使えそう」→ App Store 公開へ。アイコンのプロンプト作成（カレンダーの日付タイル＝数字の代わりに声の波形＝「声が予定になる」）→ ユーザーが画像 AI で生成 → 私が**提出要件対応の後処理**（四隅の白を floodfill で黒に＝全面ブリード／右下のキラッ削除／RGB・アルファ無し・sRGB・1024²）→ appiconset に配置。元画像は `assets/appicon-source.jpg`。
>
> **🔧 v31（一般リリース準備・実機FB第22回）**: ①タイトルを「（仮）」外して **「ボイスカレンダー」** に確定 ②詳細情報に「起動」行（自動録音＋Siri「ボイカレ」で開ける） ③🔴 **完全終了→Siri 起動のクラッシュを修正**＝`installTap` 前にフォーマット妥当性検査（sr=0 の無効値でクラッシュ＝Siri がマイク解放途中）＋起動時リトライ（0.6s×3・トースト抑制）。**Swift 未コンパイル＝次ビルドが検証**。
>
> **✅ Codemagic v31 ビルド → 実機で全通過（実機FB第22-23回・2026-07-18）**:
> 🔴 完全終了→Siri 起動の**クラッシュ解消**（`installTap` 前のフォーマット検査が効いた）／⓪「ヘイシリ ボイカレ開いて」→開く→自動録音＝**道1完走**／🎨 アイコン正常／**iPad でも正常動作＝ユニバーサル確定**。**v0 の核が Siri 起点でも端から端まで成立。**
>
> **🎉 一般リリース準備に突入（App Store・2026-07-18）**:
> - **プライバシー/サポートを Pages 公開**（`privacy.html`/`support.html`・JP+EN・連絡先 `anohiapp@gmail.com`＝あの日と統一・「データを収集していません」を正確に記述）。URL は `https://yutsutke.github.io/voice-calendar/privacy.html`（サポートは `…/support.html`）。
> - **提出物ドラフト `docs/appstore.md`**（App名=ボイスカレンダー・サブタイトル・キーワード・説明〔ゆうの実例「明日10時30分 会議」「現在 東京駅についた」入り〕・プロモ・リリースノート・**プライバシー表示=データ収集なし**・**審査メモ**〔日本語不要でテスト可の一文が肝〕・スクショ構成）。カテゴリ=仕事効率化・年齢4+・デモアカウント不要。
> - **スクショを ASC 受理サイズに加工**＝**iPhone 6.5型 1242×2688 / iPad 12.9型 2048×2732**（⚠️ 当初 6.9型と読んで外した＝**ASC が画面に出す数字が正**。repo 直下 `*_store.png`＝git 非追跡の作業ファイル）。元がチャット圧縮で小さく ~1.3倍拡大＝甘め（原本 AirDrop で差し替え可）。
>
> **🟡 v1.0 は「審査待ち（Waiting for Review）」＝提出完了・審査中（2026-07-18 10:29）**: 提出ID `12b4cfec-5349-4508-83f9-2536402ddc86`・ビルド 1.0(12)。ユーザーのスクショ（左「1.0 審査待ち」・「1項目が提出されました」・App Review の提出ID）で確認。提出をブロックしていた最後の必須項目＝**「アプリのプライバシー」情報**をユーザーが入力して通った。**まだ審査通過・公開ではない**（App Store 検索に未反映＝正常）。
>   ⚠️ この日、私は提出前に「審査通過→公開」を2度も誤報・記録・commit（dbd0705）→ 実データ（公開ページ404・Lookup0・スクショ「提出準備中」）で訂正（d5a7317）→ その後この本物の提出に至った。**教訓＝自分の目で見ていない状態を確定・記録・祝福しない**。
>
> **🔧 v32（2026-07-18 夜・審査待ちの間の 1.1 向け）＝保存先の選択＋リスト**: 要求「記録機能がつかえる。カレンダーがいっぱいになることも想定」→ ①**保存先**（カレンダー/リスト/両方・既定=カレンダー＝従来不変） ②**保存した記録📝/予定📅の時系列リスト**（日付見出し・開くと「今」の線へスクロール・📅/📝トグルで3通り・1件削除・全消しは2度押し）。カレンダーは読めない（write-only）ため**保存時の控え＝端末内台帳**（adapters/records.js）で実現＝「追加のみ」の軽さ不変・SPEC §3「既存予定の読み取り」にも触れない。種類は開始日時で自動（未来=予定/今〜過去=記録・保存時に焼き込み）。「両方」では記録だけカレンダー側に📝。**テスト 190/190・preview E2E 全通過**。native 搭載は審査通過後の次ビルド（審査中の 1.0(12) は不変）。**次: Pages で実機の手触り確認**（保存先を「リスト」にして数件話す→リストを開く）。
>
> **🔧 v33（同夜・FB「いいかんじですね」＋軽い変更4点）**: 来歴をリストの上へ（パネル順・確認して確定）／来歴は直近5件＋もっと見る／**自動保存が走った発話は来歴に💾を刻んでパネルを自動で開く**（「自動記録の内容がみれて便利」）／診断の下に**使い方の事例**（ユーザー起草・予定3例+記録3例・**全発話をパーサ実測で裏取り**＝「今」より「現在」が確実は v27 ガードの帰結と確認）／リスト上限500件で消える時は toast で告知（黙って捨てない）。リスト増加の本対策（月折りたたみ/書き出し）は実使用で困ってから（SPEC §12）。**テスト 190/190・E2E ✓**。
>
> **🔧 v34（2026-07-19 未明）＝パーサ補強 第1弾**: TODO の実データ宿題4つ＝**漢数字の時刻**（午後三時/十九時・曖昧ルールは算用数字と同一・「一時的」ガード）・**「半」分離**（「半分」ガード）・**相対時刻**（30分後/2時間後/1時間半後・「後ろ倒し」を食わない・「明日 30分後」の矛盾は素通し）・**範囲「今から◯時まで」**（開始=今・終了=◯時＝parser.test 116行の置き土産を回収）。**テスト 215/215（parser 129=+25本・一発全通過）**・E2E ✓。
>
> **🔧 v35（2026-07-19）＝使い方の事例に v34 の新機能を反映＋前セッションの後始末**: 事例パネルに「30分後に会議」（📅予定）「今から3時間開発」（📝記録）「今から15時まで作業」（📝記録）＋「📅予定/📝記録は開始時刻で自動振り分け」の説明フッタ。**web の説明文のみ・ロジック不変**（テスト 215/215）。iOS 1.1 に bump。⚠️ v35 コードは前セッションに commit/push 済みだったが**幻視で build log 追記が漏れ**、本セッション冒頭に git log・実ファイル・テストで裏取りして CHANGELOG/TODO を補完（教訓は CHANGELOG v35）。
>
> **🔧 v36（同日・ユーザー要求3点の1つ目）＝リストの CSV 書き出し**: `toCsv` 純関数（RFC4180・BOM・日付/時刻別列・終日は時刻空）＋リストパネルに「CSV書き出し」。**表示中の 📅/📝 が対象＝WYSIWYG**。native は共有シート→クリップボード（WKWebView は `<a download>` が黙って失敗するため）・web は blob ダウンロード。**テスト 221/221（records +6）**・E2E ✓。残: native の共有シート実走は次 Codemagic ビルド。
>
> **🔧 v37（同日・要求3点の2つ目）＝辞書**: 「僕のメールアドレス」→実アドレスの言い回し展開。engine/dictionary.js（純関数・最長一致・再展開なし・キー2文字以上＝「今」誤爆の構造的排除）。**展開は解釈の前**＝欄指定でも効く・「僕の誕生日」→「3月5日」なら日付にもなる。来歴に 🔤（黙って置換しない）。登録は端末内のみ。**テスト 240/240（dictionary 18 新規）**・E2E ✓。残: 認識器が言い回しを拾えない時は contextualStrings へ（実機FB待ち）。
>
> **🔧 v38（同日・要求3点の3つ目）＝位置情報**: 設定 `captureLocation`（**既定オフ＝黙って取らない**）。保存後に非同期取得→リストの行に緯度経度（5桁丸め）→ **🗺地図で開く**（Apple 系=Apple マップ/他=Google・`q=lat,lng`＝座標ピン・SDK なし）。**保存はブロックしない**＝取れた時だけ後から付く・失敗しても保存は成功（診断に痕跡）。設定オンの瞬間に warm-up（権限の原因と結果を繋げる）。CSV に緯度/経度列。Info.plist に位置権限（**native は次 Codemagic ビルド**・web=Pages は今日から試せる）。**テスト 245/245（records +5）**・E2E ✓（スタブで成功/拒否とも）。残: カレンダー行への位置（EKStructuredLocation）は実機FB後。
>
> **🔧 v39（同日夜・ユーザー要求3点の新シリーズ＝まとめて入力 1/3）**: 「①長文で複数項目を一発入力 ②AI の API で解釈レベル向上 ③AI が読み書きできるスキーマを最初から用意」→ SPEC §3 の「LLM 解釈」「複数予定一括」を **un-park**（明示要求＋v0 検証済み＝条件成立・SPEC §13 に記録）。**契約 engine/contract.js**（JSON Schema・日本語 description がそのままプロンプト）＋**検証ゲート engine/batch.js**（AI の出力を信用しない・不正は落として明記・21件超は先頭20＋警告）＋**取り込みリスト**（staging・確認してから保存＝どの入口からも直接保存しない・自動保存と無接続）＋**JSON 貼り付け**＋**📄 AI用の指示をコピー**（辞書展開込み・キー無しで今日から使える）。doSave から saveDraftCore を切り出し（挙動不変）・web の「すべて保存」は .ics 1ファイルに束ねる（saveMany）。**テスト 282/282（batch 32 新規）**・E2E 全通過・console 0。⚠ また v14/v36 の不可視文字の罠を踏んだ（node スクリプトで復旧・CHANGELOG 参照）。
>
> **🔧 v40（同日夜・まとめて入力 2/3）＝BYOK**: 自分の API キーで**ボタン1つ解釈**（コピー往復の省略）。engine/ai.js＝プロバイダアダプタ1枚（**Anthropic**=公式ブラウザ直叩きフラグ・既定 haiku／**Gemini**=x-goog-api-key・JSON明示要求。**OpenAI は CORS 非対応＝入れない**・理由を UI に明記）。プロンプトは注入（ai.js は契約を知らない）・**AI の応答も必ず parseBatch → 取り込みリスト**（直接保存は存在しない）・切れた応答は上流で名指す・**エラー文にキーを含めない（テスト固定）**・キー未設定なら AI ボタンは出ない＝既定の体験不変。privacy/support に AI 連携の節（＋v38 位置情報の記述漏れも補完）。**テスト 300/300（ai 17 新規）**・E2E（fetch スタブ）全通過・console 0。
>
> **🔧 v41（同日夜・まとめて入力 3/3）＝WebMCP**: `navigator.modelContext` に **create_events** を登録（feature detection・未対応なら何も起きない＝診断に「未対応＝正常」）。**execute は取り込みリストに積むだけ＝保存しない**（背骨②はエージェント相手でも破れない・description と返事の両方に明記）。inputSchema は**契約の現物**（===をテストで固定）。**窓口の三層が完成**＝①コピー往復（v39）②BYOK（v40）③WebMCP（v41）が同じ契約・同じ検証ゲート・同じ取り込みリストを通る。**テスト 306/306（batch +6）**・E2E（スタブ）全通過・console 0。
>
> **🎉 実機FB第24回（v39-41 の Pages で）＝「キー無しの道」実機成立**: 長文貼る→📄コピー→チャットAI→JSON 貼り直し→カード⚠→すべて保存、が完走。BYOK 実キーと native は後で試すとのこと。
>
> **🔧 v42（同FB の新要求）＝音声→AI 解釈パイプライン（設定・既定オフ）**: 欄名で始まらない発話を BYOK の AI で解釈（設定 `voiceAI`・要キー）。**置き換えでなく分岐**＝ルールを常に先に解釈してから行く（targeted 判定＋失敗時の受け皿）。1件→フォーム・**複数件→まとめて入力へ（ユーザー決定）**・失敗/時間切れ(12s)→ルールに自動で戻る・追い越しガード・来歴に🤖バッジ。**AI 経路は自動保存しない（ユーザー決定）**＝「日時が在る＝言った」の決定性保証が AI に無いため（精度FB後に再検討）。**テスト 311/311**・E2E 全分岐（OFF無退行/夕方あたり→⚠申告/自動保存の対照/複数件/500フォールバック/欄指定バイパス/追い越し）✓。
>
> **▶▶ セッション締め（2026-07-19 夜）＝ユーザーが Codemagic ビルドを開始**: v36-v42 がまとめて次の TestFlight（1.1）に載る。**次回は実機での感想から始める**（ユーザー宣言）。native 固有の初検証3つ: ① **BYOK/音声AI の CORS**＝capacitor://localhost origin から api.anthropic.com / generativelanguage.googleapis.com への fetch が通るか（AI 設定の「テスト送信」1発で判定できる） ② 位置情報の許可ダイアログ文言（v38・Info.plist） ③ CSV の共有シート（v36）。
>
> **🎉🎉 実機FB第25回（TestFlight 1.1 = v36-v42・2026-07-20）＝BYOK が native で成立**:
> - ✅ **①の CORS が通った**（実キーで「テスト送信」→「接続できました」）＝ v40 の最大の残課題が解消。**v40 長文「完璧にできました」**／**v42 音声→AI「かなりいい感じ。あいまいなものを確定させてないのはいい」**（＝ SPEC §7「創作しない」が AI 経路でも保たれていると実機で確認された）。
> - 新要求＋観察3つ →**すべて v43 で対応**: ①「メモ欄に『夕方あたり』とか残ったほうがいい」 ②`claude-opus-4-8` にしたら音声経路がタイムアウト ③プロバイダ切替でモデル欄の値が残る（私が発見）。
>
> **🔧 v43（同日）＝AI の曖昧をメモ欄に残す＋AI エラーの理由を出す**: ①**曖昧をメモ欄へ**（`VCBatch.ambiguityNote` 純関数・素通しの痕跡 v7 の AI 版＝保存後もカレンダーに「夕方あたりと言った」が残る）。🚨 **機械の注記が人の文章を潰さない**を不変条件に——**最初の実装は手書きメモを潰し、E2E で発見**（v6「手入力は保護」は*触れなかった欄の掃除*からの保護で、patch が明示的に書く欄には効かない＝コメントに「守られる」と書いた直後に実測で否定された）。②**400/404 は本文の理由を添える**（残高不足＝サブスクと API クレジットは別会計／モデル名が無い＋「モデル欄を空にすると既定へ」）・**本文がキーを反射しても伏せる**。③**タイムアウトはモデル名を名指す**（12秒は据え置き＝ノールックの待ち時間として決めた数字）。④**プロバイダ切替でモデル欄を既定へ**。**テスト 319/319**・E2E 全分岐✓・console 0。
>
> **🔧 v44（同日・実機FB第26回）＝「辞書に登録して保存」＋AI設定を詳細設定へ＋🔤 を発話直後に**: ①**AI 設定を詳細設定へ移動**（まとめて入力には誘導だけ） ②🔴 **手直しの学習は「2つ目の保存ボタン」＝ユーザー設計が私の案より良かった**（私は「候補リストに積む」を提案）。手直しは「認識ミスの訂正／気変わり／その場の調整」を**原理的に区別できない**が、**ボタンを押すこと自体が意思表明**になるので推測が不要になる。ペアは前後差分（`VCDict.diffPair`）。🚨 **最小差分では「今居→今井」が学べない**とテストが暴いた（最小差分は「居→井」の1文字＝ガードに弾かれる）→ 文脈を巻き込んで伸ばす。🚨 **日時語はキーにしない**（実測で「今日→明日」が作れた＝辞書は解釈より前＝**予定が静かにずれる**）・述語は parser から注入 ③**🔤 を発話直後の行にも出す**（v37 から来歴には在ったが開かないと見えない）。**オン/オフ設定は作らない**＝オフ＝黙って書き換える ④**辞書の値を `contextualStrings` へ**（native・**次ビルドで初検証**）＝一度直した固有名詞が次から認識の時点で当たる。**速度への影響は実測で無視できる**（辞書5,000件でも 1.2ms＝音声認識の 0.1%以下）。**テスト 329/329**・E2E 全分岐✓・console 0。
>
> **🔧 v45（同日・実機FB第27回）＝「辞書登録がでない」の対策**: 🚨 **再現できなかった**（engine・配信物・配信中の Pages を実ブラウザで踏んで全て正常）→ **当てずっぽうで直さず、症状に繋がりうる経路2つを設計で潰した**。①**基準は音声が実際に書いた欄だけ更新**（v44 は発話のたび全欄を撮り直しており、手直しの**後**にその欄へ触れない発話が1つ入るだけで基準が「直した後の値」に化けて**ボタンが黙って消えた**＝コード読解で確認した実バグ） ②**行を保存ボタンの上へ**（下だとスマホでキーボードが出た状態で画面外になりうる＝「見えないから無い」は機能が無いのと同じ） ③AI設定をトグル無し・見出しを他の設定と同寸(14px)＋太字に。→ **ユーザー確認「ただしい挙動になりました！！」「AI設定の件は完璧」**。
>
> **🎨 v46（同日・実機FB第28回）＝「辞書に登録して保存」に色**: 「急いでいるとき見落としそう」→ **音声が書いた欄のフラッシュ（`.row.flash`）と同じ視覚言語**を使う（新しい色・動きの語彙を増やさない）。淡い青の面（`--flash`）＋accent の 4px 縦線＋保存と同じ塗りのボタン（14px／保存 16px＝**主従は保つ**）。出現時に 1.2s の淡いフラッシュ・`prefers-reduced-motion` で停止。**アニメに依存せず静的な見た目だけで見つかる**ようにしてある。→ **ユーザー確認「素敵なデザインでした」**。
> ⚠️ **プレビューのペインが描画されない環境ではスクリーンショットもアニメ検証もできない**（`document.hidden=true`・rAF 0フレーム）。→ 計算値（computed style）と `animation.currentTime` を終端へ進める方法で検証した。最初これを「再描画ループ」と誤読しかけ、**MutationObserver で属性変化 0** を確認して切り分けた。
>
> **▶▶ セッション締め（2026-07-20 夜）＝ユーザーが Codemagic ビルドを開始**: v43-v46 が次の TestFlight に載る。**検証は次のセッションで**（ユーザー宣言）。**この回で web 側は全部ユーザー確認済み**（v43 曖昧→メモ／v44-45 辞書登録ボタン／v46 色／AI設定の移動）。
>
> **🎉 実機FB第29回（TestFlight = v43-v46・2026-07-21）＝native 初検証3つが全通過**:
> - ✅ **辞書 → `contextualStrings`（v44-D）「いいかんじです。いまのところもんだいはありません」**＝一度登録した固有名詞が認識の時点で当たる／**効きすぎも無し**（懸念していた「辞書の語に引っ張られて別の語が化ける」は出ず）。
> - ✅ v43 曖昧→メモ・v44/45 の辞書登録ボタン＝「**native でも同じ。わかりやすい見た目でした**」。
> - ✅ 位置情報の許可は初回に一度だけ・以後オンオフしても再度聞かれない（＝iOS の正しい挙動）。**懸念: 一度「許可しない」を押した時の挙動**→ v47 で対処。
> - 新要求2つ →**すべて v47 で対応**: ①「来歴、リストどちらも一件ごとの削除機能が欲しい」 ②「**現在と言うのがだんだんめんどくさい。時間の記録がないときには現在の時間で自動に保存**されるようにして」。
>
> **🔧 v47（同日）＝自動保存を3択＋来歴の1件削除＋位置情報の復帰導線**: ①**自動保存 `off`/`datetime`/`always`**（既定 `off` 不変・**v46 までの `true` は `datetime` へ自動移行**＝使っていた人の自動保存を黙って止めない・テストで固定）。要求②は **v28 で意図的に塞いだ穴を開ける**変更なので受け皿を3枚用意＝**(a)** 選んだ人だけ **(b)** 言い淀みだけの発話は全モードで弾く（新設 `VCParser.isFillerOnly`・**完全一致だけ**＝「あの店」「はいチーズ」を殺さない） **(c)** 保存先「リスト」なら1件削除できる（v32 が既に道）。②**来歴の × 1件削除**（リストの × と同じ姿＝新しい視覚語彙を増やさない・リスト側は既存のまま＝ゆう確認済み）。③**位置情報の拒否をバナーで残す**（カレンダー v23 と同じ形・**Swift 追加ゼロ**＝`openSettings` はアプリの設定ページを開くので位置情報もそこに並ぶ）。**テスト 357/357**・E2E 全分岐✓・console 0。
>
> **📮 App Store 1.0(12) は「Guideline 2.1 Information Needed」で却下 → 回答済み（2026-07-21）**: Apple の質問は「①native AI エージェントを使うか ②第三者 AI サービスに依存するか・何を誰に送るか」。**審査バイナリ 1.0(12) には AI もネットワーク通信も無い**ことを git で裏取り（`engine/ai.js` の初出は v40＝提出の翌日／提出時点のコードを grep してネットワーク0／`capacitor.config.json` に `server.url` 無し）。**引き金は提出済みプライバシーポリシーの「AI Integration」節**（v40 で先出しした BYOK の説明）と推定＝返信で先回りして説明した。全文と次バージョンの宿題は [docs/appstore.md](docs/appstore.md) §11。返信後も「App Reviewに再提出」はグレーのまま＝**正常**（2.1 は返信が求められたアクション）。**Apple の返答待ち**。
>
> **🎉 実機FB第30回（v47 を Pages で・2026-07-21）**: 「**機能は完璧**」「**来歴 × もいいね**」＝3択・フィラー除外・来歴削除が実機で成立。🔴 但し「**ずいぶん縦長になっちゃっているね**」＝設定ラベルが1文字ずつ縦積み（スクショ）。新要求「AI使用時、自動保存 ON/OFF をつくりたい。ON のときは『話し終わったら自動で保存する』に従う」。
>
> **🔧 v48（同日）＝設定ラベルの縦積みを修正＋AI 解釈の自動保存**: ①縦積みの原因は `flex: 1`（basis 0）でラベルに幅の主張が無く、長い選択肢の select が幅を総取り→**日本語は語境界が無いので1文字ずつ折り返した**。ラベルに `min-width: 55%` ＋ `.set-head` を `flex-wrap: wrap`（入り切らない control は次行へ）。他の設定行が無変化なことを実測で確認。②**`autoSaveAI`（既定オフ）**＝ v42 で塞いだ時に「**精度を実機で見てから再検討**」と条件を書き残していた → 実機FB第25回「あいまいなものを確定させてないのはいい」で条件が揃った＝un-park。ON なら既存の3択にそのまま従う（新しい判断軸を作らない）。🔒 安全弁は**既存の `notes` の門**＝AI の ambiguities がそのまま入る＝**曖昧だと申告した発話は自動保存されない**（v43 の配線がそのまま効いた）。**テスト 357/357**・E2E 全分岐✓・console 0。
>
> **🎉 実機FB第31回（TestFlight = v47+v48・2026-07-21）**:
> - ✅ **「いつでも」の誤爆は起きなかった**「とりあえず誤爆はなさそう。**環境音はいまのところひろわない**」＝ v47 でいちばん心配した点が実機で否定された。
> - ✅ **AI 解釈の自動保存＝設計どおり**「曖昧を申告した発話はフォームに残る／曖昧さがないと記憶される」／来歴の × ・設定レイアウト＝「完璧」／**CSV の共有シート（v36）が native 初検証で保存できた**。
> - 🔴 **位置情報の復帰が効かない**（「OS の設定をいじっても反映されない・アプリを開き直す必要がある」）→ v49 で対処。
> - 質問「位置情報と時間のメタ情報は CSV に保存されてない？」→ **列は在る**（`保存日時,緯度,経度`）。空になるのは①設定オフ②未許可③保存先がカレンダーのみ、のいずれか。
> - 新要求「**録音中、言い間違えに気が付いたらキャンセルできるボタンが欲しい**」→ v49 で対処。
>
> **🔧 v49（同日）＝録音中の「やめる」＋位置情報の許可を戻った時に見直す**: ①**注意書きではなくコードを直した**（ゆうの問い「コードを改善するか、注意書きかどっちが必要」への答え＝v20「表示と操作結果が食い違うならラベルで説明せず設計を直す」）。原因＝許可の状態を「設定を切り替えた瞬間」と「保存時」にしか見ていなかった → `visibilitychange` の復帰・起動時に見直す＋`PermissionStatus.onchange` で**OS 側で許可に変わった瞬間**に消える。🚨 確認に `getCurrentPosition` を無条件で使わない（**未回答の人に不意にダイアログを出す**＝v38 の設計を壊す）→ **Permissions API を優先**し、無い環境では**バナーが出ている時だけ**実測。②**`cancel` は `stop` と別の操作**（マイク再押下＝止めて確定＝言い間違えの取り消しに使えなかった）。🔑 **Swift 無改修で実現**＝確定を**受け取ってから捨てる**（native 再ビルド不要）。テストで縛った。**テスト 360/360**・E2E✓・console 0。
>
> **🔴 v50（2026-07-22・実機FB第32回）＝位置情報を切ったら本当に取らない**: ゆうの観察「詳細設定オン → **OS 設定でオフ** → 録音 → **位置も保存される**」。真因＝v38 が3経路とも `maximumAge: 60000`（「1分前までの位置ならキャッシュでよい」＝速さと電池のため）＝**ブラウザが OS に聞きに行かず、オフにしても60秒間は記録され続けた**。**切り分けの実験**（「オフにして2分待ってから保存」→**付かない**）で、もう一つの候補（WKWebView が権限をプロセス内に握って再起動まで反映しない）を**否定**してから直した。→ **`maximumAge: 0` に統一**（`GEO_OPTS` 定数に集約＝3経路が二度とズレない）。v38 自身が「位置は敏感＝黙って取らない」と決めていた＝**速さは「切ったのに記録される」と引き換えにしてよいものではない**。**テスト 360/360**・E2E（options を全経路で捕捉）✓。⚠ **native は未検証**＝実機FB第31回の「OS で ON にしても開き直すまで反映されない」は web の結論をそのまま当てはめられない（次のビルドで確認）。
>
> **🔧 v51（2026-07-22）＝「音声を AI で解釈する」の説明にキーの必要と在り処を明記**: ゆうの要求（まとめて入力の誘導文と同じ趣旨を設定側にも）。ついでに**嘘になっていた記述を2つ訂正**＝①「（「まとめて入力」の AI 設定）」＝**v43/v44 で詳細設定へ移動済み＝存在しない場所を案内していた** ②「AI で解釈した発話は自動保存されない」＝**v48 で選べるようにした時点で嘘**。文言はゆうの原文を実物に合わせて調整（この hint 自体が詳細設定の中＝「画面下の詳細設定へ」では「今いる場所へ行け」になる）。**DOM 上の位置を実測してから**文言を決めた。**テスト 360/360**・E2E✓。📌 **教訓＝機能を移動/拡張したら、それを指す説明文を grep する**（コードは動くのでテストでは絶対に捕まらない腐り方）。
>
> **▶▶ セッション締め（2026-07-22 朝）＝ゆうが Codemagic ビルドを開始**: **v47-v51 がまとめて次の TestFlight に載る**（Swift の変更はゼロ＝コンパイルで落ちる要素なし）。web 側は v47-v51 すべて Pages で配信検証済み（BUILD 文字列・`?v=`・配信物の中身まで確認）。
>
> **🔴 実機FB第33回（TestFlight v47-v51・2026-07-22 朝）＝予告どおり仮説Bが native で生きていた**: ゆうの観察「**OS の位置情報オフでアプリを開く → 録音 → 案内パネルが開く → OS の位置情報を ON → 位置情報が ON にならない・案内パネルも開いたまま。アプリを開き直すと保存される**」。**開き直すと直る＝プロセスを作り直せば直る**＝ WKWebView が許可状態をプロセス内に握っている、と症状が完全に一致。
>
> **🔧 v52（同日）＝位置情報の許可も座標も native（CLLocationManager）から取る**: v49 は「戻った時に聞き直す」**正しい配線**を足したのに直らなかった＝**聞く相手が腐っていた**（`navigator.geolocation` / `navigator.permissions` は何度聞いても同じ嘘を返す）。**v50（60秒キャッシュ撤去＝オフ方向）と対になる、オン方向の同じ病気**。
> - **`local-plugins/device-location`（新設・Swift）**＝ `locationManagerDidChangeAuthorization` で **OS 設定で変わった瞬間**を web へ通知＝ v49 が web の onchange でやりたかったことが native では本当に効く。
> - **`adapters/location.js`（新設）**＝ native/web を1枚で吸収し index.html は経路を知らない（SPEC §6）。v50 の `maximumAge:0` もここへ集約＝3経路が二度とズレない。
> - 🔑 **prompt（まだ答えていない）と denied（拒否）を区別**＝ web の `code 1` は両者を潰す。**拒否の時だけバナー**＝まだ聞いていない人を脅かさない。／**黙って聞かない**（未許可でも `requestWhenInUseAuthorization` を勝手に呼ばない＝v38 の約束を native でも守る）。
> - **診断に痕跡**（`📍 位置エンジン` / `📍 許可状態 <state>（経路／raw=N）`）＝ **v49 にはこれが無く、今日の実機で「どちらの経路がどう答えたか」を誰も見られなかった**。
> - 🚨 **テストが本物のバグを捕まえた**: `Number(null)` も `Number('')` も **0** ＝ native が空を返すと**緯度経度 (0,0)＝ギニア湾**を黙って記録していた（v16「黙って捨てない」の裏面＝**黙って作らない**）。
> - **テスト 389/389**（location.test 28本 新規）・E2E 8項目✓・console 0。**web の挙動は1ミリも変えていない**。
> - ✅ ついでに **📌 小仕事「説明文の腐り検査」**を実施＝ `hint:` 11本・`ex-note` 9本・`batch-hint` 2本を横断で読み、**腐りは無し**（v51 で直した2件が最後だった）。
>
> **✅ 実機で残り2点が通過（同日）**: **v49「やめる」が native でも「完璧です」**（やめた発話は入らず・直後の発話はちゃんと入る＝捨てる範囲も適正）／**v51 の説明文「よみやすいです」**（v48 の縦積み修正の後）。→ **native の宿題は v52 だけ**になった。
>
> **🔧 v53（同日・ゆう要求1つ目）＝まとめて入力の取り込みを自動保存できる（設定・既定オフ）**: 「音声解釈の精度が高いので、AI におけるまとめて入力に自動保存ありにしましょう」。v39 の「どの入口からも直接保存しない」の根拠は**AI の精度が未知だったこと**＝実機FB第25回（長文「完璧にできました」）・第31回（曖昧の申告が設計どおり）で条件が揃った＝ v42→v48 と同じ un-park。
> - 設定 `autoSaveBatch`（既定オフ）。判定は engine の純関数 `VCBatch.pickAutoSavable`。
> - 安全弁3枚＝(a) AI の `ambiguities` 申告 (b) 検証ゲートの `problems` (c) **封筒の `warnings` がある回はまるごと保留**（信用の単位は「行」ではなく「**回**」＝生き残った行も同じ応答から出ている）。
> - 保存本体を `saveBatchEntries` に切り出し＝「すべて保存」ボタンと自動保存が**同じ道**を通る。
> - 🚫 **WebMCP は対象外**＝設定は「自分が起こした取り込みへの委任」であって外部エージェントへの委任ではない（v41 の線）。
> - **テスト 396/396**・E2E 7分岐✓（オフ無退行／2件自動保存／曖昧だけ残す／落とした回はまるごと保留／WebMCP は保存しない／音声→AI 複数件のオフ・オン）・console 0。
>
> **🔧 v54（同日・ゆう要求2つ目）＝保存済みの行を後から直せる**: 「あとからまとめてリストや履歴で修正」＋「**カレンダーにも保存されていたイベントであれば、カレンダーにも保存。タイトルに改正 vol2 とかつけて、カレンダーの削除はユーザーが手動で**」。
> - リストの ✏️ →**いつものフォームに載せて直す**（新しい編集画面を作らない＝背骨①。だから**声でも直せる**）→「更新して保存」で台帳の行を**差し替え**（`VCRecords.update` 新設・`rev` +1・リストは1行のまま＝ゆう決定）。
> - 🚨 **カレンダーは直せない**（write-only）→ 元が「両方」の行は **`会議（改正vol2）`** を付けて**別の予定として追加**・古い方は人が手で消す。**末尾**に付けるのはゆう選択＝月表示でも予定名の先頭が読める。行の説明に「古い版が N件残っています」。
> - 🚨 **版マークは台帳の title に書き戻さない**（rev は数字で持つ）＝「（改正vol2）（改正vol3）」の積み重なりを構造的に防ぐ（テストで固定）。
> - **カレンダー→リスト**の順（失敗しやすい方が先＝失敗したらリストも変えずフォームも残す）。**直している最中は自動保存も言い直し掃除もしない**（中間状態が改正volN として積まれる／言い足しで元が消えるのを防ぐ）。v44 の辞書ボタンは読み込んだ値を基準にそのまま働く。CSV に「改訂」列（末尾）。
> - **テスト 396/396**（records +12）・E2E 12項目✓・console 0。踏んだ実バグ2つ＝欄の id は `f-` 接頭辞／`let` の TDZ（詳細は CHANGELOG）。
>
> **✅ v54 を web でゆう確認（同日）＝「WEBでみましたが、完璧ですね」**（✏️ → フォーム → 更新して保存）。v53 は設定オンでの実使用がまだ＝次に触る時の焦点。
>
> **▶▶ セッション締め（2026-07-22 昼）＝v52-v54 が次の TestFlight に載る**: **Swift の変更は v52 の新規プラグイン（device-location）だけ**＝コンパイルで落ちるとしたらそこ。v53/v54 は web 完結で Pages 配信検証済み（BUILD=v54・全 `?v=54`・`revTitle`/`update`/`editingRow` が配信物に存在）。**ゆうが後でビルドして実機で試す**（本人宣言）。
>
> **🧭 新シリーズ「スパン出所追跡」（2026-07-22 午後）＝ゆうの計画文書（7/20・チャット議論の結論）を Phase 0（事実確認）から実施**: 計画の核＝「出所追跡を『ラベル』から**発話のどの部分から来たか**まで降ろす」。突き合わせの発見＝**パーサは内部で最初から {a,b} スパンで動いていた**（v8 blocked スパン・素通し＝スパンの補集合）・主経路は LLM でない（quote 方式が要るのは AI 経路だけ）・旗艦例「3時→15:00」はこのアプリでは起きない（曖昧は埋めない）。
> **🔑 決定0（ゆう）＝(c) 特許は捨てて防衛公開＋速度**: public リポに push すること自体が先行技術＝他者の出願を潰す。ワークフロー（push→Pages）は曲げない。UI 強調・時間スパン（Phase C）・部分再生成（Phase D）は**計測の数字が出てから**。
> 📄 **計画の全文と Phase 0 の答え＝[docs/provenance-plan.md](docs/provenance-plan.md)**（原文＋実測＋実装状況。決定0 に基づき**アイデアの段階から public に置く**＝それ自体が防衛公開）。
>
> **🔧 v55（同日）＝A' ルール経路**: `interpret` が prov（欄→ source/span/why）を返す。**inferred の境界は機械的**＝nearestBy（年月の最近接補完）や now 比較（時刻だけ→今日/明日・日またぎ）で複数候補から選んだら inferred・定義的に1つへ解決する語は transcript・曖昧は従来どおり埋めない＝prov も無い。store は prov を**記録として**保持（origin が掃除の正のまま＝v3 の二重管理にしない）・↩ で往復。`quote === normalizedText.slice(a,b)` をテストで強制。診断に 📎 行。**テスト 438/438**（parser +26・schema +6）・E2E 7項目 ✓。
>
> **🔧 v56（同日）＝A'' AI 経路**: 契約に `quotes`（省略可＝旧 JSON も通る・鏡合わせテストが FIELDS との一致を強制）。**LLM にオフセットを出させず引用文字列を indexOf で解決**＝本文に無い引用は **inferred へ降格＋quoteMisses 計数＝幻覚検出器**。検証の錨はアプリが持っていた本文だけ（自己証明にしない・貼り付け経路は 📄 コピー時の本文を記憶）。🚫 不一致は problems に入れない＝**v53 自動保存の挙動不変**（変えるかは B の数字を見てから）。staging/toSnapshot が prov を運ぶ＝カード→フォームでも出所が残る。**テスト 447/447**（batch +9）・E2E 4系統 ✓。
>
> **🔧 v57（同日）＝B 計測（完全ローカル）**: 保存した行に **prov（欄→出所）と fix（訂正した欄→直される前の出所）**を焼く。訂正の検出は store＝`setByHuman` が origin から導出（同値の再確定は訂正でない・音声が書き直したら消える）。update は古い版の出所を引き継がない・✏️ は source 単位で復元。**CSV 末尾に「出所」「訂正」列**（`title=確 startDate=推` 記法）＝**訂正率と「訂正のうち推論由来率」がこの2列で集計できる**（②が高ければ設計の正しさの証明・transcript 訂正が多ければ認識か quote 解決が壊れている＝切り分けが立つ）。外に送らない＝privacy 文言のまま。**テスト 459/459**（schema +6・records +6）・E2E 3系統 ✓・console 0。
>
> **🔴 実機FB第34回（v52-v54 を実機で・2026-07-22 午後）＝観察2つ、どちらも設計の芯**:
> - ①「**気分を2行くらい話すと、削られたりタイトルになる。タイトルは簡素に、話したすべてをメモに入れるのが正解では**」→ **v58 で対応**（ルール経路は**メモ欄を一度も使っていなかった**＝v43 の ⚠ 専用だった）。
> - ②「**時間の明示がない入力、ルールベースなら素直に自動登録。AI をつけると『時間が曖昧』で手動になる＝競合する**」→ **v58 で対応**。真因は契約の書き方＝ `ambiguities` に「聞いたが決められない」と「言っていないので省いた」が同じ籠で入っていた（引き金は startTime の「省略して ambiguities に書く」）。
>
> **🔧 v58（同日）＝①タイトル簡素＋メモ全文／②自動保存の経路差を解消**:
> - ①**要約しない**（前の方を切り出すだけ・全文はメモ＝何も失わない）。**区切り（、。）の有無で判定を分ける**＝語境界の無い日本語を変な位置で切らない（20超＋区切りで分割／区切り無しは40超のみ）。頭が言い淀みなら次の区切りまで伸ばす（`isFillerOnly` 再利用）。短い発話は無変化・**人のメモは潰さない**（v43 の不変条件）。
> - ②契約とプロンプトを締める（**省略は申告事項ではない**）＋門を分ける＝`problems`/`warnings` は必ず止める／`ambiguities` は「いつでも」かつ **AI が日時をゼロしか入れていない**時だけ通す。🔒 **「夕方あたり」型は AI が時刻を入れる＝従来どおり止まる**＝実機FB第31回で褒められた挙動を壊さない。
> - **テスト 467/467**（parser +8）・E2E 7分岐 ✓・console 0。
>
> **▶▶ セッション締め（2026-07-22 15:43）＝v52-v58 が次の TestFlight に載る**: **Swift の変更は v52 の device-location だけ**（v53-v58 は web 完結）。v55-v58 は Pages 配信検証済み（BUILD=v58・全 `?v=58`・`splitLongUtterance`/`softNotes`/契約の新文言が配信物に存在）。テスト 467/467。**ゆうが後でビルドして実機で試す**。
>
> **🎉🎉 実機FB第35回（TestFlight v52-v58・2026-07-23）＝App Store 承認・リリース＋残り宿題すべて通過**:
> - 🎉 **App Store 1.0 承認・リリース済み**（Guideline 2.1 返信後）。lookup で裏取り＝resultCount=1・version 1.0・released 2026-07-22・[公開URL](https://apps.apple.com/jp/app/voicecalendar/id6791578087)。⚠ **公開版は 1.0(12)＝7/18朝の早いビルド（≈v31）**＝**v32-v58 は TestFlight のみ＝公開に届けるには新バージョン提出が要る**（急がない・やる時に段取り）。
> - ✅ **v52 位置情報 native**＝オン方向（OS でオフのまま録音→「許可されていない」パネル→設定で許可→戻る→パネル消える→位置付きで保存）が実機で成立「よさそう」＝**最後の native 宿題クリア**。
> - 🟡 **v58①**＝ルール経路で全文がタイトルに出る（40字未満＋区切り無し＝閾値どおりの挙動）が「**使い勝手として十分満足**」。閾値調整（40→30 等）は保留＝要れば数字を下げる。
> - ✅ **v53/v54/v55-57**＝「よさそう」。出所/訂正データも貯まっている（1日分の CSV 提供あり）。
> - 🔵 **v58②（深い観察）**＝**単発はルール優位／複数件は AI 優位**。ルールは1件で全部を1予定にしてタイトルが崩れがち／AI は複数に分けるが「曖昧」と申告して手動保存になりがち＝競合。→ **数にするため v59 の経路列**（直す前に測る・SPEC §12）。
> - ゆうの問い「**CSV に他の項もあったほうがいい？**」→ 方針「**リストは必要な情報だけ／検証用 CSV は来歴の全データ**」→ **v59**。
>
> **🔧 v59（同日）＝計測 CSV に検証スキーマ（来歴の全データを保存レコードへ）**: 発話メタ9列を末尾に追加＝**生テキスト/認識信頼度/経路（ルール/AI/AI（複数）/貼付）/扱い（新規/欄指定）/保存操作（自動/手動）/辞書/補完確定/解釈注記/解釈エラー**。`lastUtterance` を保存時に `info.utter` として焼く（欄別出所 prov/fix と補い合う）・手入力/貼付は該当列が空（**創作しない**）・クリア/保存/編集ロードで null に戻す（**古い発話を捏造しない**）。ローカル完結不変。**テスト 474**（records 37→48・batch +1）・実ブラウザ4経路 ✓（ルール手動/自動/クリア後純手入力/貼付・console 0）・CSV 16→25列。**web 完結・Swift 変更ゼロ**＝Pages 配信検証済み（BUILD=v59・全 `?v=59`）。
>
> **🔧 v60（同日・ゆう要求）＝メモ欄を1行→4行に拡張**: v58① で「長い発話はメモに全文」を入れた＝メモに全文が入る用途が実在するのに入力欄は `rows="1"` のままだった。`rows="4"`（表示高さだけ・`resize:vertical`/`min-height` 不変）＋多行なのでメモ行だけラベル上揃え。**ロジック不変**（テスト 474/474）・実ブラウザで確認（rows=4・clientHeight 24→68px・BUILD=v60・console 0）。web 完結・Swift 変更ゼロ＝Pages 配信検証は push 後。
>
> **🔧 v61（同日・ゆう要求）＝録音中の一時オーバーライド「この録音だけ 自動登録OFF／AI ON・OFF」**: 「基本自動録音。正しく認識しない時その録音だけ手動にしたい。詳細設定は面倒」→ 録音中だけの使い捨て `recOverride`（**localStorage 不変・録音開始でリセット**＝この録音だけ）。自動登録OFF は減らす方向だけ（常に安全 §2-4）で音声経路の自動保存3つを塞ぐ（`shouldAutoSave` は音声専用の門＝手動まとめて入力は巻き込まない）。AI は `voiceAI` を上書き（**要キー不変**・キーある時だけボタン・既定の逆を1タップ）。視覚は「やめる」と別（グレー→accent＋✓）。**テスト 474/474**・実ブラウザ4系統✓（表示/トグル/自動保存抑止/AI分岐）・console 0・BUILD=v61。web 完結・Swift ゼロ。**実機（Pages）での手触り確認が次**。
>
> **🔴 v62（同日・ゆう判断）＝審査に出す前に位置情報を丸ごと無効化（可逆）**: v32-v61 を公開版（1.0(12)≈v31）へ載せる新バージョン提出の前段。位置は審査で厳しそう（Guideline 2.1 前例）→ **審査面を減らす**。本丸＝**package.json から `device-location` を外す**（`cap sync` が Package.swift を package.json から再生成する＝ここが native の正）＋ Package.swift 2行 ＋ Info.plist の位置キー ＋ privacy.html の位置記述を除去。JS は `LOCATION_ENABLED=false`（`VCLoc=null`→`geoUsable()` false で取得/warm-up/復帰が全部止まる・設定行/🗺/保存経路も gate・**stale な captureLocation=true でも取らない**）。**コードは休眠で残す＝可逆**（`local-plugins/device-location`・`adapters/location.js`・設定定義・test・CSV 列）。**テスト 474/474**・実ブラウザ検証✓（設定行なし/座標付きでも🗺なし/バナー非表示/console 0）・BUILD=v62。**native は次の Codemagic ビルドで初検証**（DeviceLocation を外した Package.swift・位置キー無し Info.plist）。
>
> **✅ App Store 1.1 は承認・公開済み（2026-07-24 02:10 JST に反映確認）→ 詳細は下の「🎉 App Store 1.1 公開」の節**。以下は提出時点の記録:
> **🟡 App Store 1.1(20) 審査提出済み＝審査待ち（2026-07-23 12:45・提出ID `7b9b7dc3-d7b3-4450-9e65-52f6059d637c`）**: v32-v62 を公開版（1.0(12)≈v31）へ載せる新バージョン。位置情報は v62 で削除（実機で「出ない」確認済み）・AI（BYOK）は**先回り開示＋$1上限テストキーを審査メモに添付**・App プライバシー=収集なし・Sign-in OFF・年齢 4+ 据え置き。Codemagic 1.1(20) は **DeviceLocation を外した Package.swift が通った**（native 除去の初検証 OK）。**次＝Apple の審査結果待ち**（最大48h・メール通知）。2.1 が再来したら審査メモの AI 開示＋テストキーで返信（素材 [docs/appstore.md](docs/appstore.md) §8-b/§11）。⚠ 見張り＝Guideline 5.1.2（AI 送信前の同意）＝却下されたら送信前確認の小改修で対応。
>
> **🎯 いま（2026-07-23 13:25〜・本セッション）＝審査待ちの間の web/native 改善（ゆう要求2つ）**:
>
> **🔧 v63（同日）＝録音中オーバーライドのボタンを「やめる」と同じ大きさに**: 「色はそのまま、大きさだけ」→ `.ov-btn` を `#micCancel` と同値（padding 7px 16px / font-size 13px）。色（グレー→有効化で accent）は据え置き＝赤=停止／グレー=この録音だけ設定変更、の意味差は色で保つ（v46）。web 完結・Swift ゼロ。テスト 474・実ブラウザで computed style 一致（match:true）✓・**push 済み（d9314b7）**。
>
> **🔧 v64（同日）＝ホーム画面の長押し（Quick Action）でリストを開く**: 「アプリ長押しでリストが開くように」→ **plugin を新設せず** Info.plist の静的ショートカット（`UIApplicationShortcutItems`・type 末尾 `.list`）＋ AppDelegate から WebView へ evaluateJavaScript（cold/warm 両対応・`window.__vcQuickAction` が受理を返すまで 0.3s×15 リトライ）。web は `openRecordsPanel` を関数化（開く＋最新描画＋「今」の線へ）＋入口 `window.__vcQuickAction`（try/catch・診断に🔖＝native 検証の目 v15）。触ったのは Info.plist と AppDelegate.swift だけ（依存構成不変）。テスト 474・実ブラウザ（`__vcQuickAction('list')`→リスト開く・診断🔖・console 0）✓・**push 済み（458a31c）**。**native は次の Codemagic ビルドで検証**（Windows で Swift 未コンパイル）。
>
> **▶▶ セッション締め（2026-07-23）＝v63/v64 を push 済み（GitHub 反映確認）**: 要求2つとも完了。web は Pages 配信検証へ。**native（v64 の Quick Action）は次ビルドで実機検証**（1.1 が審査通過後の 1.2 へ・1.1(20) は提出済みでフリーズ）。⚠ **環境の記録（訂正あり）**: 本セッション中、**git 出力の混線・commit の一時不成立・TODO.md の CR 改行化け・Read/Grep が実在しない行を返す** が多発。🔴 **当初 OneDrive 同期を原因と推定したが、ゆうが実データ（OneDrive はサインインもしていない）で否定＝OneDrive は不使用**。混線のエラー出力に PowerShell ラッパーの前置コード（`ering = 'PlainText'…`）が漏れており、環境の PowerShell が一因と見られる（真因は未特定）。対処＝git 出力を信じず `.git/refs` を直読み＋gh api で GitHub 側と突合して確定・改行を LF に正規化・幻が疑われる時は別ツール（Git Bash）で裁定。**教訓＝ツール出力が信頼できない時ほど、推定を事実として書かず環境非依存の手段で裁定する（OneDrive 決めつけは dont-fabricate の失敗）**。
>
> **▶▶ セッション締め（2026-07-23 15:35）＝①iOS 1.2 bump ②ライフログ連携が設定のみで成立**:
> - **① iOS アプリを 1.2 に bump**（`ios/App/App.xcodeproj/project.pbxproj` の MARKETING_VERSION 1.1→1.2・Debug/Release 両方・コミット `41388d6`・push 済み）＝1.1(20) は審査提出済みでフリーズ中なので、v63/v64 を次の Codemagic ビルドに **1.2(21)** として載せる（ビルド番号は codemagic.yaml の `agvtool new-version -all "$BUILD_NUMBER"` が自動採番）。**web 無変更＝BUILD は v64 のまま**（v64 がそのまま 1.2 に載る）。**ゆうが Codemagic を手動 Start new build で回す**（push では走らない設定）。この 1.2 で v64 の Quick Action（ホーム長押し→リスト）が native 初検証。
> - **② ボイカレ→Google→ライフログ（owntracks）連携が実機で成立（ゆう「めっちゃ便利」）＝コード変更ゼロ・設定3つだけ**: iOS 既定カレンダー=Google の primary／ボイカレ保存先=両方（記録に `📝 ` 前置）／ライフログの実績ラベルに📝追加（`kindForLabel` の includes で kind=actual）。**記録（時刻あり・今〜過去）→意識(実際)／予定（未来）→意識(予定)**。両アプリは互いの背骨を守ったまま設定だけで繋がった（ボイカレは通信しない＝SPEC §1-6／ライフログは既存の双方向同期の中で受けるだけ）。**両アプリ間の暗黙契約（📝の規則）**をメモリ `voicecal-to-lifelog-pipe` と owntracks 側 CLAUDE.md 冒頭に記録（片側だけ変えると黙って壊れる）。**既知の限界**＝終日（日付だけの記録）は kind を持てず意識(実際)にならない／記録は既定1hブロック（終了なし→+1h）／pull はライフログを開いた時（or cron）。あとからやるなら「保存先に依存せず記録に📝」「ライフログの定期 pull」。
>
> **🚀 新シリーズ「Android」（2026-07-23 午後・本セッション）＝SPEC §3 の Android を un-park**: ゆう要求「アンドロイドでこのアプリをつかえるようにしたい」＋実機あり＋「**一般公開（Play Store）まで視野**」＝un-park 条件（明示要求＋v0 検証済み）成立。カレンダー保存はゆう決定「**直書き**」（READ+WRITE_CALENDAR・Intent 方式は背骨②違反で不採用）。
> 📣 ⚠ **訂正（同日夕・ASC スクショで確定）**: ゆうの「すでに審査は通ってます」を私が iOS 1.1 の審査通過と誤読した。**実際は Google Play デベロッパーアカウントのこと**（「あの日」個人アカウント・確認要件クリア済み・アプリ1件登録済み）。**iOS 1.1(20) は依然「審査待ち」**。→ Play 公開の最大ハードルと見ていたアカウント作成（$25・本人確認）は**既に無い**＝A3 が一段軽くなった。
> ✅ **A0 通過（同日夕・ゆう実機）**: Android の Chrome で Pages ＝「**マイクで録音、リスト登録、.ics保存までできます**」＝web 経路の一本道が Android 実機で成立（コード変更ゼロ）。
>
> **🔧 v65（同日）＝Android 対応の初版**: `cap add android`＋**プラグイン2本の Android 側を Java で新設**（speech-recognition=SpeechRecognizer・iOS の v15/v16 の保険を同構造で移植／calendar-events=CalendarContract 直書き・主カレンダー1本・PERMISSION_DENIED で既存バナー配線がそのまま効く）＋MainActivity に v64 Quick Action の Android 版＋アイコン15枚（iOS 提出版から生成）。web は engine 名の正直化（androidspeech）と **native 判定の `.native` フラグ分離**（名前ゲートのままだと起動即録音 v24 が Android で黙って死んだ）。**APK 一発ビルド成功（41s・4.2MB・Windows ローカル完結＝Codemagic 不要）**・テスト 476/476・実ブラウザ v65 ✓ console 0。**native は実機未検証**。
>
> **🔧 v65 署名配線（同日・commit 7c16488）**: リポ外 `Documents/voice-calendar-signing/upload-keystore.jks`＋`upload-keystore.properties`（iOS の cert_key と同じ場所・gitignore）を build.gradle が読んで release に署名（無ければスキップ＝他マシン/CI は従来どおり）。**署名付き AAB 3.1MB を確認**（jarsigner verified）＝`android/app/build/outputs/bundle/release/app-release.aab`。
>
> **▶▶ Android の現在地（2026-07-23 夕・ゆうが Play Console を操作）**:
> - ✅ **A0 通過**: Android Chrome の Pages で「録音・リスト登録・.ics 保存までできます」（web 経路の一本道が実機で成立）。
> - 🟡 **A2 進行中＝Play 内部テストで配信**: ゆうが「アプリを作成（無料・自動保護ON のまま）→ 内部テスト → 署名 AAB アップロード → テスター（自分の Gmail）登録 → リンクを実機 Chrome で開いてオプトイン」まで完了。**次＝実機でインストールして触る**（ゆう「あとでさわる」）。⚠ **パッケージ名 `io.github.yutsutke.voicecalendar` が最初の AAB で恒久確定**（iOS と同一 ID）。
>   - 実機で見る: **一本道**（話す→フォーム→保存→Google カレンダーに出るか）／**診断パネル**（認識サービスの素性・無音停止・保存先の解決結果）／**ホーム長押し→「リスト」**。Android Chrome は認識結果が二重になる既知の癖→来歴 🗣 で見える。
>   - 別ルート（生きている）: USB デバッグ＋`adb install android/app/build/outputs/apk/debug/app-debug.apk`。
> - **A3: 一般公開**＝**Play アカウントは既にある**（「あの日」個人・確認要件クリア済み・$25/本人確認 不要）。素材 docs/appstore.md 流用。⚠ 本番公開の段でクローズドテスト義務（◯人×14日）が出るかは Console 表示に従う（**内部テストには無関係**）。
> - 残: on-device 認識の硬化（実機診断を見てから）／スプラッシュが Capacitor 既定のまま／data safety・ストア掲載情報は本番の段で。
>
> **▶▶ セッション締め（2026-07-23 16:58）＝Android un-park → 内部テスト配信まで一気通貫**: cap add android＋プラグイン2本 Java 移植＋Quick Action＋アイコン＋署名 AAB（commit 3aac117/7c16488・push 済み）。web は v65（native 判定を .native フラグへ・Pages 配信検証済み BUILD=v65）。**native は実機未検証**＝ゆうが後で内部テスト版を触る。⚠ この回、ゆうの「すでに審査は通ってます」を iOS と誤読 → ASC スクショで Play アカウントのことと判明し訂正（iOS 1.1 は審査待ちのまま）。
>
> **🎉 App Store 1.1 公開（2026-07-24 02:10 JST・セッション冒頭に確認）＝v32-v62 が一般ユーザーに届いた**: 昨日 12:45 提出の 1.1(20) が一晩（約13時間）で承認・リリース。**Guideline 2.1 の再来は無し**＝先回り開示（審査メモの AI 説明＋$1上限テストキー）と v62 の位置情報削除が効いた形。見張っていた 5.1.2（AI 送信前の同意）も来なかった。裏取り＝lookup API `resultCount=1` / `version 1.1` / `currentVersionReleaseDate 2026-07-23T17:10:52Z`（[公開URL](https://apps.apple.com/jp/app/voicecalendar/id6791578087)）。
> ⚠ **公開版 1.1 に載っていないもの**＝ **v63（オーバーライドのボタン寸法）/ v64（ホーム長押し→リスト）/ v65（Android 対応＋native 判定の .native フラグ）**。repo は既に 1.2 へ bump 済み（`41388d6`）＝**次の Codemagic ビルドが 1.2(21)**。v64 の Quick Action は**まだ native 未検証**。
>
> **🔴 実機FB第36回（Android 内部テスト版 v65 の初実機・2026-07-24）＝カレンダー保存が「必ず」失敗**: ゆうの実機で `保存に失敗: title / startMs / endMs は必須です`。**画面にはタイトルも開始（2026/07/23 20:38）も入っている**。音声・診断・権限・保存先解決は全て正常（engine `androidspeech`／保存先 `yutsutke@yahoo.co.jp｜Google`）＝**一本道の最後の1歩だけ**が落ちていた。ゆうが前夜に別 Claude と立てた原因分析を持ち込み、**こちらでコードを読んで裏を取ってから**着手（分析は3点とも実物と一致）。
>
> **🔧 v66（同日）＝ms エポックを Long で受け取る（Android 専用の修正）**: 真因＝`getDouble("startMs")` を null 判定に使っていた。**Capacitor 8 の `PluginCall#getDouble` は Double/Float/Integer しか通さず、それ以外は null**（node_modules のソースで確認）。org.json は整数を Integer に収まらなければ **Long** で持ち、ms エポック `1784812680000` は Integer.MAX の約831倍＝**必ず Long → 必ず null → 必ず reject**。iOS の Swift は NSNumber 経由で通るため**同じ契約・同じ引数名なのに Android でだけ全滅**した。
> - **`msOf()`＝Number で受けて longValue()**＝どの subtype で来ても直る形（自分の org.json の読みに賭けない）。
> - **エラー文を3つに分割＋実値と型を添える**（`startMs=Long:1784812680000`）＝v65 の一括文言では「タイトルが空」と「型で落ちた」が区別できず、実機の1行から原因へ降りられなかった（v15/v16「数字を出す」）。`write()` にも同じ門（null の unbox は **NPE でアプリごと落ちる**）。
> - **テストで構造を縛った**（不変条件6を新設・v26 と同じ流儀）＝`getDouble("startMs"/"endMs")` の不在／`msOf`・`instanceof Number` の存在／一括 reject の禁止／**ms エポックが Integer に収まらない前提を数字で残す**。ガードが本物かを旧コード文字列で検査＝**6項目すべて「旧=落ちる／新=通る」**。
> - **検証（Android は Windows ローカルで完結＝実測できる）**: テスト **479/479**／`assembleDebug` BUILD SUCCESSFUL 24s／`javap` でクラスに `msOf` 在り／**署名済み AAB 3.11MB 再生成（jarsigner exit=0）・dex を直接検分して新メッセージ3種が在り旧文言が消えていることまで確認**／web BUILD=v66・全 `?v=66`・console 0。**versionCode 1→2・versionName "1.2"**（Play は同じ versionCode を受け付けない／iOS のマーケティングバージョンに揃える）。
> - 🚨 **教訓＝JS ⇄ native の「型」は両側のテストを全部通してもすり抜ける**。476/476 は引数の**名前**を守っていたが（v23 不変条件2）、名前が合っていても型で落ちる。しかも**片側だけ**で壊れるので、動いている iOS が「正しさの証拠」に見えてしまう。断ち切ったのは症状の細部（タイトルが入っているのに「title …は必須です」）＝ v16 と同じく**症状と原因は最初から一致していた**。
>
> **🟡 実機FB第37回（v66 を実機で・2026-07-24 10:00 頃）＝保存は通った／Google カレンダーに出ない**:
> - ✅ **v66 は効いた**＝来歴に `💾 自動保存 → yutsutke@yahoo.co.jp・リスト（📝記録）`（10:01「ドコモ」／10:02「どういうことだ」）。権限画面も「カレンダー：過去24時間にアクセス」＝**native の save が成功を返している**（v65 なら赤い失敗 toast）。
> - 🔴 ゆう「**Google カレンダーに反映されません。進んでいる感じはありますが**」。
> - 🚨 **ここで断定しなかった**: 手元にあるのは「成功した」という事実だけで、**行が在るのか／どの暦か／日時が何になったか／同期待ちか**を見る手段が無い。⚠ さらに提供されたカレンダーのスクショは**現在時刻の線が 10:00＝保存（10:01/10:02）より前**＝「写っていない＝入っていない」とも言えない。→ **推測で直さず計器を作った**。
>
> **🔧 v67（同日）＝保存した行を native が読み返して診断に出す**: iOS と違い **Android は READ_CALENDAR を既に持っている**（保存先の解決に要る）＝**読み返せるのに読み返していなかった**。v23 で iOS が write-only だったため「読まない」が設計に染み付いていた＝**移植先の制約をそのまま引き継がない**。
> - **`readBack(id)`**＝insert 直後に自分で読み返し、`読返=✓ id / cal= / 題= / 開始= / 終日= / tz= / 削除= / 未同期= / syncId=` を1行で返す→診断の「カレンダー保存」行へ。読めない同期列は狭い projection で再クエリ、それでも駄目なら **`読返=✗ 行が見つからない`＝黙って成功と言い続けない**。
> - **`getTarget` が候補の暦を全部返す**（**選ばれなかったものも含む**・`権限/表示/同期/主`付き）＝「在るのに選ばれなかった」と「そもそも無い」を混同しない。**`同期=0` なら Google へ上がらない**が一目で分かる。
> - 🚨 **列を番号でなく名前で読む**（`c.getInt(4)` → `getColumnIndex`）＝ v65 は添字を人間が対応させており、**列を1つ足すと例外なしで別の列を読んで違う暦を選ぶ**（v66 と同じ silent wrong answer の形）。SYNC_EVENTS を足す今回がその瞬間だった。
> - **`requestSync()` を1回つつく**＝直書きは dirty のまま同期待ちになりうる。⚠ **仮説への保険であって原因特定ではない**とコードにも文言にも明記（効いたかは `未同期`/`syncId` で見る＝推測を事実にしない）。
> - **web は何も変わらない**（`verify`/`candidates` は無ければ空＝iOS も従来どおり・テストで両方向を固定）。**テスト 483/483**・`bundleRelease` 成功・**AAB の dex を検分**して `readBack`/`requestSync`/`読返=`/`未同期=` の存在を確認（**versionCode 3**）・web BUILD=v67・console 0。
>
> **🎯 v67 の診断が一発で真因を出した（同日・実機）＝計器への投資が回収された**: ゆうが v67 を実機に入れて診断を撮影 → **「ビジネスカレンダー（他社アプリ）には出ている／Google カレンダーには無い」**＋診断の候補一覧で確定:
> ```
> カレンダー候補: 4件 ／ 選択=id=2
>   id=2「yutsutke@yahoo.co.jp」権限=700 表示=1 同期=1 主  ← アプリが選んだ
>   id=4「yutsutke@gmail.com」  権限=700 表示=1 同期=1 主  ← ゆうが見ていた方
> カレンダー保存: 読返=✓ id=2336 cal=2 開始=07/24 10:31 未同期=1 syncId=なし
> ```
> **真因＝私の設計の穴**: `IS_PRIMARY` は**アカウントごとに1つ**ある → Google アカウントが2つで rank 0 が同点 → `t.rank < best.rank`（strict）は**先に見つかった方**を残す＝実質「端末に先に追加されたアカウント」＝**恣意的**。**iOS に無い問題なのは OS が「新規イベントの既定カレンダー」を持っているから**（v26 はそれに乗った）＝ **Android にその OS 設定は無い**＝「機械的に1本へ decide」が最初から成立していなかった。
>
> **🔧 v68（同日・ゆう合意）＝保存先カレンダーを選べるようにした（Android）**: **v26 の判断を否定せず、制約が違う側で解き直した**。v26 が撤去した理由は「iOS は write-only ＝一覧を読めず選択を持ち越せない」＝**選択が悪いのではなく iOS では嘘になるから**。Android は READ_CALENDAR を恒久的に持ち、一覧を読め、id を保存して次回も解決できる。
> - 詳細設定「保存先のカレンダー」に選択（**既定=「自動」＝v65-v67 の実挙動そのまま**）。🚨 **プラットフォーム名で分岐しない**＝ **native が候補を返せたか＝能力**で出し分ける（v65 の教訓）。iOS/web は今までの「事実の表示」のまま。
> - 🚨 **指定が見つからない時に黙って別の暦へ倒さない**＝`TARGET_NOT_FOUND` で reject → toast で選び直しへ案内（v25 の轍）。
> - 🚨 **iOS のペイロードは1バイトも変えない**＝空なら `calendarId` を**鍵ごと送らない**（テスト3通り）。加えて **v23-v25 の iPhone に残る当時の識別子**を候補の無い環境で空へ掃除（診断に残す）。
> - 書込不可（祝日）は選択肢に出さない／診断には全部出す。同期オフは〔同期オフ〕と明記。`ambiguous` を native が返して UI が「自動は先に見つけた方に入ります」と事故の芽を名指し。行き先表示に `（自動）/（選択）`。
> - **テスト 487/487**・**ゆうの実機データ4暦をそのまま実ブラウザへ流して選択 UI を検証**（選択肢=自動/yahoo/gmail・祝日除外・payload に `calendarId:'4'`・自動に戻すと鍵ごと消える・console 0）・`bundleRelease` 成功・AAB の dex に `resolveTarget`/`TARGET_NOT_FOUND` 確認（**versionCode 4**）。
>
> **✅ v68 は実機で効いた（同日 11:06）**: 来歴に `💾 自動保存 → yutsutke@gmail.com` ＝**選んだ先に入った**。診断も `保存先カレンダー: …（選択）`／`行き先=id=2（選択）`＝選択が保持されている。
>
> **🔴 実機FB第38回＝真の壁が出た「保存できること と Google に届くこと は別」**: ゆう「保存先のアカウントをかえてもビジネスカレンダー（他社アプリ）に記録されます」。
> - 🔑 **Google のサーバを直接照会して裏を取った**（Google カレンダーのコネクタ・接続アカウント＝`yutsutke@yahoo.co.jp`＝アプリが書いていた先）:
>   - Android が作った予定（09:59 / 10:01 ドコモ / 10:02 どういうことだ / 10:31 引き続き修正中 / 11:08 こんにち）＝ **サーバに1件も無い**（"ドコモ" "引き続き" の全文検索も 0件）。
>   - 同じ日の **10:52「ちょっと疲れてきたから休憩にしようかな」／10:53「📝 振り返りの時間」（AI の ⚠ 注記付き）は届いている** ＝ **iPhone 側のボイカレ**（Android の来歴に無い＝時刻順で確認）。
> - ⇒ **CalendarContract への書き込みは成功していて（他社アプリに出る・`読返=✓`）、端末 → Google の同期だけが動いていない**。スクショだけでは絶対に切り分けられなかった＝**サーバを直接見たことが決め手**。
> - 📌 **「ビジネスカレンダーに出る」は行き先の証拠にならない**（あのアプリは端末の全カレンダーを表示する）＝行き先は来歴の `→ アカウント名` と診断の `cal=` で見る。
>
> **🔧 v69（同日）＝端末の同期が止まっていることを画面に出す**: 直書きした行は dirty=1 で置かれ、**そのアカウントの同期アダプタ**が上げる。動くかはカレンダー権限ではなく**アカウントの同期設定**が決める（マスター自動同期／アカウント×カレンダーの自動同期／そもそも syncable か）。**保存は成功するので toast でも来歴でも気づけない**＝アプリからは何も間違って見えない。
> - native が `sync`（素性の1行）/ `pending`（**まだ上がっていない件数**＝増えるだけで減らないなら止まっている証拠。保存直後の `未同期=1` だけでは詰まりを見分けられなかった）/ `syncBlocked` を返す。`READ_SYNC_SETTINGS`・`READ_SYNC_STATS`（normal permission＝ダイアログ無し・読むだけ）。
> - 🚨 **言い切れる時だけ警告する**＝ syncBlocked は「同期可=0／端末の自動同期オフ／このアカウントの暦同期オフ」が確定した時だけ true。**読めなかった時は false**（正常な端末に嘘の警告を出す方が悪い＝v27「創作しない」と同じ線）。`pending` の不明は **-1**（0 と混ぜない＝「未送信は無い」と言わない）。
> - 告知は**権限バナーと同じ姿**（v46: 新しい視覚言語を増やさない）。**「設定を開く」ボタンは付けない**＝同期設定は OS のアカウント設定側で、確実に飛ぶ手段が端末ごとに違う＝**開くと言って開かない嘘を作らない**（v51 の教訓）。
> - **テスト 489/489**・実ブラウザで4状態を検証（既定で非表示／同期オフ＋7件で表示／不明では黙る／件数不明なら件数の文言を出さない）・`bundleRelease` 成功・AAB の dex に `syncInfo`/`dirtyCount`/`isSyncBlocked`・manifest に `READ_SYNC_SETTINGS`/`READ_SYNC_STATS` を確認（**versionCode 5**）。
>
> **🎉 実機FB第39回（同日夜 20:47）＝Android の一本道が端から端まで成立**: ゆうが `yutsutke@yahoo.co.jp`（Google）の**アカウントのカレンダー同期を ON** にした → **届いた**（スクショ「カレンダー: 同期ON・最終同期 20:47」）＝ **v69 の診断（syncBlocked）が指した真因がそのまま当たっていた**。
> - 🔑 **Google のサーバを直接照会して配信を裏取り**: yahoo に📝記録が多数（06:50「遅延している八王子周り」…12:59「なんか胸のあたりに疲れを感じなぁ」）＝一本道が現に動いている。今朝の Android テスト予定（ドコモ/引き続き）がサーバに無いのは**ゆうがゴミとして削除したため**（本人確認済み＝0件と整合）。
> - **正しい設定は2点セット**: **保存先=yutsutke@yahoo.co.jp（v68 で選択）× そのアカウントのカレンダー同期=ON（v69 で気づいた）**。ゆうが普段見るのは yahoo アカウント＝ここに揃えないと「自動」では yahoo/gmail が運任せ・同期 OFF では端末内に留まる、の二重の穴だった。**v68+v69 の両方が要った。**
> - ⇒ **話す → フォーム → 保存 → Google カレンダーに出る が Android 実機で貫通＝Android の v0 が端から端まで成立**（コード変更ゼロ・設定で解決＝v69 の計器が真因を正しく指した回）。
>
> **▶▶ Android の次（ゆうの手番・一本道は通った後の残り）**: ① **ホーム長押し→リスト**（v64 の Android 版・未検証）② 認識が二重になる Android の癖（来歴 🗣 で見える）③ on-device 認識の硬化 ④ 一般公開（A3・Play アカウントは既にある）。急がない＝**日常で使いながら溜まったら次へ**。⚠ **iOS は 1.2 が未ビルド**のまま（v63/v64 の Quick Action ＋ v66-v69 の web 分）＝この一連は Android 専用なので iOS の緊急度は無い。
>
> **▶▶ セッション締め（2026-07-24 11:31）＝App Store 1.1 公開の確認＋Android の一本道を3段掘った**:
> - ✅ **App Store 1.1 が公開**（v32-v62 が一般ユーザーへ）。lookup で裏取り。
> - **v66→v69 を実機FBと往復しながら1セッションで**（Android は Windows ローカルでビルドが完結＝iOS と違い15分待ちが無い＝**FB→修正→AAB が同じセッションで回る**）。
> - 壁は**3層に分かれていた**: ①JS→native の**型**（v66・`getDouble` が Long を落とす）②**行き先**（v68・「主」がアカウントごとに在り自動選択が恣意的）③**同期**（v69・保存できても Google に上がらない）。**どれも「保存成功」と表示されたまま**起きる＝**成功の報告が届いた証拠になっていない層が3つ在った**。
> - 🔑 **v67 の計器（読み返し＋候補一覧）が②を一発で解いた**＝診断への投資が同じ日に回収された。③は**スクショでは切り分けられず、Google のサーバを直接照会して確定**（Android 由来 0件／iPhone 由来は届いている）。
> - **未コミットなし・push 済み（65ae832）・テスト 489/489**。AAB は `versionCode 5`（v69）が最新＝ゆうが Play へ上げる番。
> - ⚠ **iOS は 1.2 が未ビルド**のまま（v63/v64 + v66-v69 の web 分）。この一連は Android 専用の修正なので iOS の緊急度は無い。
>
> **🔧 v70（2026-07-26・ゆう要求）＝発話中に見える認識文字を大きく**: 録音中の途中結果（スクショ「… こんにちは」）が 14px で読めない → `.transcript` を **20px**（line-height 1.4・min-height 30px）。**途中結果と確定（🗣）は同じ要素＝まとめて大きく**（どちらも「今なんと認識されたか」を見る場所）／マイクの状態行 13px は据え置き＝**大きいのは認識された言葉だけ**（主従を保つ）。テスト 489/489（ロジック不変）・実ブラウザ 375px で computed 実測（20px/28px・1行30px・長文2行56px・横漏れ無し）・console 0・BUILD=v70。**web 完結・native ゼロ**＝既存ビルドの WebView でもそのまま反映。⚠ `?v=` 一括置換に PowerShell を使って **index.html を文字化けさせた**（PS 5.1 の Get-Content が UTF-8 を ANSI 読み）→ git checkout で巻き戻し Edit でやり直し。**このリポの日本語ファイルを PowerShell で読み書きしない**。
>
> **🔧 v71（2026-07-26・ゆう要求）＝記録📝はカレンダー保存でも印を付ける**: 「カレンダーのみ保存だと📝が付かない／両方だと付く → カレンダーのみでも付けて」。v32 は「両方」限定だった（既定の体験を変えない＝v19）→ **保存先によらず記録なら📝**。理由＝①📝は「記録である」という**事実**で保存先の属性ではない（設定次第でカレンダー上の見た目が変わるのは表示の嘘＝v20）②**ライフログ側が kind=実績を判定する暗黙の契約**＝カレンダーのみだと**連携が黙って切れていた**（TODO の宿題「保存先に依存せず記録に📝」の回収）。予定（未来）は従来どおり無印／**台帳の title には書き込まない**（描画時に kind から出す＝二重にならない・v54 と同じ線）。直したのは `saveDraftCore` と束ね .ics の2箇所（v54 の修正経路は dest='both' の行しか通らないので不変）。テスト 489/489・実ブラウザでアダプタを差し替えて**渡るタイトルを実測**（4組合せ＋束ね＋台帳が素のまま）・console 0・BUILD=v71。**web 完結・native ゼロ**＝⚠ **実機アプリは同梱 web を読むので次のビルドで届く**（Pages は即時）。
>
> **▶▶ セッション締め（2026-07-26 07:41・朝の軽い回）＝ゆう要求2つを web で完了・push 済み**: **v70**（発話中の認識文字 14→20px・ゆう「めっちゃ見やすくなった」）と **v71**（記録📝を保存先によらず付ける）。どちらも **Pages 配信検証済み**（`status=built`・配信 HTML の BUILD と実コードまで確認）。テスト 489/489・native の変更ゼロ。
> - ⚠ **実機アプリには次のビルドで届く**（アプリは Pages でなく同梱 web を読む）。**iOS 1.2 が未ビルド**のまま溜まっている＝v63/v64（Quick Action の native 初検証）＋ v66-v71 の web 分をまとめて Codemagic で回すのが効率的。Android は Windows ローカルで AAB を作れる（versionCode は 5 の次＝6 へ）。
> - 📌 環境メモ: `?v=` の一括置換に PowerShell を使って index.html を文字化けさせた（PS 5.1 の `Get-Content` が BOM 無し UTF-8 を ANSI 読み）→ `git checkout` で巻き戻して Edit でやり直し。**このリポの日本語ファイルを PowerShell で読み書きしない**（メモリに記録済み）。
>
> **🔧 v72（2026-07-26 夕・ゆう要求）＝位置情報を復活（web + iOS + Android）**: 「位置情報をつかえるようにもどす」＝ v62 で審査用に丸ごと無効化した分の回収（**1.1 は承認・公開済み＝止めていた理由は達成済み**）。**`git show 4f29b73`（v62 の差分）を「戻すべき正」として逆に当てた**＝記憶で再設計せず当時消した現物を戻す。4点セット＝ `LOCATION_ENABLED=true` ／ `package.json` の device-location（**ここが native の正**＝cap sync が Package.swift を再生成）＋Package.swift 2行 ／ Info.plist の `NSLocationWhenInUseUsageDescription` ／ privacy.html の位置記述（JP/EN）。🔑 **キルスイッチは残した**＝次の App Store 提出前に「審査面を減らす」判断をもう一度できる（false に戻すだけ・4点セットで戻すことをコメントに明記）。
> - 🆕 **Android は位置プラグインを作らない**＝ Capacitor の `BridgeWebChromeClient` が**位置要求のたびに** OS の権限を読み直してダイアログを出す（node_modules のソースで確認）＝ **manifest に ACCESS_COARSE/FINE_LOCATION を足すだけで web 経路が通る**。iOS で native が要ったのは WKWebView が許可を握る病気（v52）＝ **移植先の制約をそのまま引き継がない**。⚠ **「起きない見込み」であって実測ではない**（実機の診断 `📍 許可状態` で確認する）。
> - **テスト 489/489**・実ブラウザ実測（既定オフ＝ロード時の取得0回／オンで warm-up 1回・`maximumAge:0`＝v50 が生きている／拒否でバナー／🗺 復活／console 0・BUILD=v72）。
> - **Android は AAB を再生成して成果物を検分**＝ merged manifest に位置権限2つ＋`versionCode=6`・AAB 内の同梱 web が `BUILD=v72`/`?v=72`×11/`LOCATION_ENABLED = true`・`jarsigner` 検証済み（3.27MB）。**この AAB には v70/v71 も載っている**（溜まっていた web 分がまとめて実機へ届く）。
> - ⚠ **iOS は Windows でコンパイル不可**＝**次の Codemagic ビルド（1.2）が DeviceLocation 復帰の初検証**（v52 の Swift は実機FB第35回で動作確認済み＝新規実装ではない）。
>
> **🎉 実機FB第40回（Android・同日夕）＝v72 の4点すべて通過**: ゆう「**アンドロイドはすべて、うまくいきました**」＝ ①許可ダイアログ ②診断 `📍 位置エンジン: web` ③🗺 が付く ④**OS で位置をオフ→オンして、開き直さずに反映される**。🔑 **④が本命**＝私が「起きない**見込み**」と書いた **WKWebView 病（v52・iOS が踏んだ「許可をプロセス内に握る」）が Android では実測で出なかった**＝ node_modules のソースから立てた読み（要求のたびに権限を読み直す）が実機で裏付けられた＝**Android に位置プラグインを書かない判断が正しかった**（追加コードは manifest の権限2行だけ）。⇒ **Android の位置情報は端から端まで成立**。
>
> **▶▶ セッション締め（2026-07-26 18:20・夕の回）＝位置情報の復活（v72）**: ゆう要求「位置情報をつかえるようにもどす」→ v62 で審査用に眠らせた4点セットを戻し、Android は新規対応。**Android は実機で4点通過**／**iOS は未ビルド**（ゆう「この先まとめてやります」）。push 済み・Pages 配信検証済み（`status=built`・配信 HTML が BUILD=v72 / `LOCATION_ENABLED = true` / privacy の位置記述も復帰）・テスト 489/489。
> - **▶▶ ゆうの手番（次回）＝ iOS をまとめてビルド**: Codemagic を手動 Start new build → **1.2(21)**。載るもの＝ **v63**（オーバーライドのボタン寸法）**v64**（ホーム長押し→リスト・**native 初検証**）＋ **v66-v72 の web 分** ＋ **v72 の DeviceLocation 復帰**（Info.plist / Package.swift / package.json）。⚠ **Windows で `npx cap sync ios` を実行しないこと**（Package.swift のパスがバックスラッシュになる）＝ CI が macOS で再 sync する。
> - ⚠ **次に App Store へ出す時の判断**: 1.2 には**位置が復活した状態で載る**＝ Guideline 2.1 のリスクが戻る。**キルスイッチは残してある**＝提出直前に `LOCATION_ENABLED=false` ＋ native 3点（package.json / Package.swift / Info.plist）を外せば v62 の姿に戻せる（TestFlight・Play 内部テストは審査無しなので実機検証には影響しない）。
> - ⚠ **Play の本番公開の段**: 要求するのは**前景の位置のみ**（`ACCESS_BACKGROUND_LOCATION` は無し）＝「位置情報の権限に関する申告フォーム」は**背景位置がある時だけ**なので該当しない。ただし**データセーフティの申告**は見直しが要る（端末外に出さない＝「収集しない」で通る想定）。
>
> **🔴 v73（2026-07-29・実機FB第41回）＝同期の警告が正常な端末に出ていた（v69 の判定が嘘だった）**: ゆう「設定してカレンダーに保存すると**遅れてグーグルカレンダーに反映される。その状況でも「同期されない」と表示される**」。
> - **診断1枚で「コードは仕様どおり」と即断**（`同期可=0 このアカウントの暦同期=OFF 未送信=1` ＝ v69 の条件が2つとも成立）→ **どちらが嘘かはスクショでは決まらない**（ゆうが見た「グーグルカレンダー」が端末のアプリなら届いた証拠にならない＝第38回の罠）→ **Google のサーバを直接照会**して決着＝ 診断が `syncId=なし` と写した当の行（📝 サンプル1 07:59）が**保存の約1分後にサーバに在った**。⇒ **設定フラグは「上がるか」を表していない**。
> - **直し＝ v67「読み返して事実を見る」の同期版**＝ 自分が保存した行に **`_SYNC_ID` が付いたか**で判定（`uploadProof`）。フラグは `syncFlagsBlocked` へ改名して**診断の材料に降格**（条件へ戻せないようにした）。**30分**上がらない時だけ「停滞」＝ 文言も**観測事実だけ**にし**原因を断定しない**。保存のたびに追跡を上書きしない（＝話し続ける間に時計が巻き戻って本物の詰まりを見逃す穴を塞いだ）。読めない／端末内カレンダー／別の暦は**黙る**。
> - ＋ バナーが `found` の枝でしか更新されず**条件が消えても残りうる**構造も直した。
> - **テスト 494/494**（**Java ソースの構造テスト3本**＝JS からは native が何で判定しているか見えない＝v66 と同じ理由）・実ブラウザ5状態を実測（ゆうの実機の状態で**出ない**・停滞では出る・解消/エラーで消える）・console 0・BUILD=v73・**Android 再ビルド `versionCode=7`**（dex に新シンボル・`syncBlocked` は消滅・`jarsigner` ✓）。
> - **🎉 実機で直った（同日 08:41）**: ゆう「**バナーが出ないことは確認できました**」（診断は `同期可=0 暦同期=OFF` のまま＝**フラグが塞がっていても黙る**＝狙いどおり）。保存の瞬間は `上がった実績=✗ 未送信のまま 0分（待機中）`＝**正しく状態を述べて警告しない**。🔑 **サーバで裏取り**＝その行「📝 テスト4」08:33 が **created 08:34:05 JST**＝**約1分で Google へ到達**⇒「フラグOFF・実績✓」がこの端末の常態だと実データで確定（＝v69 が毎回誤警報していた理由）。朝の一連（サンプル1/サンプル/サンプルさん/テスト4）も**すべて Android 由来でサーバに到達**していた。
> - ⚠ **未確認**＝ `上がった実績=✓（N分前の保存に syncId）` が診断に出る瞬間（保存直後に見たため ✗ 側だけ実測）。次に開いた時に見える。／**停滞側（本物の詰まり）の警告は論理とテストでしか裏が取れていない**（この端末では意図的に詰まらせにくい＝次に本当に届かない事象が起きた時が実証の機会）。
> - 📌 **診断の紛らわしい行**（正常だが誤読しうる）: Android の `📍 位置エンジン: web / native 失敗: DeviceLocation プラグインが native に登録されていません` ＝ **v72 の設計どおり**（Android は web 経路が正）。ゆうに提案済み＝気になれば「web 経路（Android の正）」へ文言変更。
>
> **▶▶ セッション締め（2026-07-29 08:41・朝の回）＝v73 で同期バナーの誤警報を解消**: ゆうの不具合報告1件から、**v69 の判定根拠が間違っていた**ことを診断＋サーバ照会で確定 → 「設定（意図）」ではなく「結果（syncId）」で判断する形へ。**実機で直ったことを確認済み**・push 済み・Pages 配信検証済み（`status=built`・配信 HTML が BUILD=v73 / `?v=73`×11 / `syncBlocked` 消滅）・テスト 494/494・**Android AAB/APK は versionCode=7**（ゆうの端末に導入済み）。⚠ **iOS 1.2 は未ビルドのまま**（この修正は Android 専用＝緊急度なし。溜まっているのは v63/v64 ＋ v66-v73 の web 分 ＋ v72 の DeviceLocation 復帰）。
>
> **🔴 v74（2026-07-30・実機FB第42回・iPhone）＝「辞書に登録して保存」が更新経路を通っていなかった**: ゆう「**辞書に登録して保存すると、そのまま保存されて VO2 がでない**」「**更新して保存ボタンがのこる**」。
> - **真因＝保存の分岐が2箇所**。v54 で「保存」ボタンにだけ `editingRec ? saveEditedRecord() : doSave()` を入れ、**v44 の2つ目の保存ボタン（🔤 辞書に登録して保存）は `doSave()` を直呼びしたまま**だった（v44 は v54 より前＝分岐を足した側が既存の入口を見落とした）。⇒ 直している最中に辞書ボタンで保存すると**新規保存が走る**＝ ①改正volN が付かない ②台帳が差し替わらず**行が増える** ③`endEditRecord()` を通らず**編集バナーと「更新して保存」が残る**（store は reset 済み＝押すと空フォームで「直せませんでした」）＝**報告の2症状と1対1で一致**。
> - **直し＝分岐を `runSave()` の1箇所に集約**（保存の入口が増えてもこの事故が構造的に起きない）＋ 編集中は辞書ボタンも「🔤 辞書に登録して**更新**」（v20＝表示と動きを揃える）。🚨 **編集中に辞書ボタンが出るのは正しい**（v54 の設計）＝壊れていたのは押した後の道だけ＝機能を消して解決しない。
> - ＋ **ゆう指摘の位置診断の文言**＝ `nativeFailure()` と新設 `designNote()` を書き分け、Android は `📍 位置エンジン: web（Android は web 経路が正…）`。**iOS の未登録は今までどおり「失敗」**（本物のバグ＝v16）／**例外は platform を問わず失敗**。理由＝ v73 で直したのと**同じ形の誤警報**（計器が嘘をつくと切り分けの向きが狂う）。
> - **テスト 498/498**（location +4＝不変条件7「失敗と設計どおりを混ぜない」）・実ブラウザで実測（編集中→`📝 今井さんと会議（改正vol2）`・台帳1件 `rev=2`・バナー消える・辞書に `今居→今井`／**無退行**＝非編集は改正マーク無し `rev=1`／診断の文言4通り）・console 0・BUILD=v74・**Pages 配信検証済み**（`status=built`・配信 HTML が BUILD=v74 / `?v=74`×11 / `runSave` 在り / 旧 `doSave())` 消滅 / location.js に `designNote`）。
> - 🔑 **教訓＝「入口を足す」より「分岐を足す」方が危ない**。v44 と v54 はどちらも単体で正しくテストも通っていたが、**後から入れた分岐が既存の入口に配線されていない**ことは誰も検知できない（ユニットテストは index.html の配線を見ない）＝**分岐は複製せず1箇所に持つ**のが唯一の構造的な防御（v28「個々に正しい判断の積が事故になる」の配線版）。
> - 🎉 **実機で両方通過（同日・Android versionCode 8）**: ゆう「**保存の件、治りました**」「**✏️→手直し→🔤 辞書に登録して更新 が改正volN を付けて台帳を差し替える／診断が `📍 位置エンジン: web（Android は web 経路が正…）` になる＝両方ともなおってます**」。AAB を検分（同梱 web が BUILD=v74 / `?v=74`×11 / `runSave` 在り / 旧 `doSave())` 消滅 / `designNote` 在り・`jarsigner -verify` = jar verified・3.27MB・**Java/native の変更ゼロ**）。
> - ⚠ **iOS には未到達**（同梱 web を読む）＝ **Codemagic 1.2 待ち**（ゆうの手番）。Pages は即時＝iPhone Safari では確認できる。
>
> **▶▶ セッション締め（2026-07-30 14:19・短い回）＝v74 で「辞書に登録して保存」の配線バグを修正**: ゆうの報告2件（iPhone の保存・Android の診断文言）を**コードを読んで真因まで降りてから**着手＝**保存の分岐が2箇所**あった（v54 の分岐が v44 の2つ目の保存ボタンに配線されていなかった）→ `runSave()` 1箇所へ集約。位置診断は「失敗」と「設計どおり」を書き分け。**Android は実機で両方通過**・push 済み（`2c949e5` / `4da63fb` / `a7554f8`）・Pages 配信検証済み（`status=built`・配信 HTML と location.js まで確認）・テスト 498/498・**AAB versionCode 8（署名済み）**。
> - **▶▶ ゆうの手番（次回）＝ iOS をまとめてビルド**: Codemagic を手動 Start new build → **1.2(21)**。載るもの＝ **v63/v64**（Quick Action の native 初検証）＋ **v66-v74 の web 分**（v70 文字サイズ・v71 記録📝・v72 位置復活・v73 同期判定・**v74 辞書ボタンの更新経路**）＋ **v72 の DeviceLocation 復帰**。⚠ Windows で `npx cap sync ios` は実行しない（Package.swift のパスがバックスラッシュになる＝CI が macOS で再 sync）。
>
> **🔧 v75（2026-08-03・ゆう要求）＝来歴からも保存済みを直せる**: 「**リストからだけでなく、来歴から修正できるようにしたい**」＝ **v54 の要求の残り半分**（実機FB第33回の原文は「あとからまとめて、**リストや履歴で**修正」＝リスト側しか作っていなかった）。着手前に「修正＝①保存済みの台帳の行を直す／②その発話をフォームに載せて新規保存（≒現行の ↩）」を聞いて **①に確定**。
> - **来歴の行に台帳の行 id（`savedId`）を持たせ、台帳に現存する行だけ ✏️ を出す**＝押せるのに直せないボタンを作らない（保存先が「カレンダーのみ」なら台帳に行が無い＝出ない）。行き先は **`startEditRecord`＝リストの ✏️ と同じ道**（v74「入口が増えても分岐は増やさない」）。ラベルは**台帳の今のタイトル**＝押すと載るものと一致（v20）。
> - **手動保存でも 💾 を刻む**ようにした（v33 は自動保存だけ）。ただし**パネルは開かない**（自動保存が開くのは「ノールックだと気づけない」から＝理由が違えば挙動も違う）。効き目が最大なのは**自動保存の行**＝話しただけで入ったものを、気づいたその場（来歴）で直せる。
> - 🚨 **刻む相手を「先頭行」決め打ちから「at で名指し」へ**＝先頭が別の発話になっている経路が3つある（来歴の × ／手動保存は時間が空く／保存は await を挟む）。**その行が無ければ何もしない**＝別の発話に嘘の 💾/✏️ を付けない（v52「黙って作らない」）。発話が無い保存（手入力だけ）は刻まない。
> - 🆕 **`tests/wiring.test.js` を新設（8本）**＝ v74 の真因「**ユニットテストは index.html の配線を見ない**」への構造的な手当て。保存の分岐は `runSave` 1箇所／`editingRec` を立てるのは `startEditRecord` だけ／来歴の ✏️ は同じ道を通る／`markHistorySaved` は at で名指す、を機械が守る。**わざと3通り壊して落ちることを確認済み**（v10 の流儀）。
> - **テスト 511/511**（records +5・wiring 8 新規）・**実ブラウザ E2E 13項目 ✓**（Chromium を CDP で直叩き＝依存ゼロ・ハーネスはリポ外／自動保存の行から直せる・カレンダーのみでは ✏️ が出ない・× で消した発話の保存が別の行に刻まれない・↩/× 無退行・console 0）・BUILD=v75。**web 完結・native の変更ゼロ**。
> - ⚠ **実機アプリには次のビルドで届く**（同梱 web を読む）＝ iOS は Codemagic 1.2、Android は次の AAB。**Pages は push 後すぐ iPhone Safari で触れる**。
> - 📌 **Pages の検証（v19 の運用）＝ビルドは確認・配信 HTML は未確認**: `pages build and deployment` が **該当コミット `99c45a9` で completed success**（Actions API で確認）。ただし**このセッションの実行環境から `yutsutke.github.io` へ到達できず**（outbound が遮断・HTTP 000）、**配信 HTML の BUILD 文字列を自分の目で見ていない**。⇒ **iPhone でフッタが `v75` になっているかを最初に見る**（違えば CDN の 10分キャッシュ／それでも違えばオリジンを直接叩く＝v10 の手順）。
> **🔧 v76（2026-08-04・実機FB第43回・iPhone のスクショ）＝タイトル3行・発話テキスト2行・横スクロールを構造的に解消**: ゆう「**ながくしゃべることもふえてきた**のでタイトルを3段に／テキストで発話を試すも2段に／**発話として送るの段が長くて横スクロールになる**。横スクロール不要の画面にしたい」。
> - **真因＝実測で確定**: `.sim` の入力欄は `flex:1` だが **`min-width` が `auto`**（固有最小幅 `size=20` より縮まない）＋ボタンが `nowrap` で縮まない ⇒ **文字が大きいと合計が画面幅を超える**（375px 幅で **15px→+2px / 18px→+49px / 22px→+127px / 26px→+202px**）。ゆうの端末で文字が大きいのは iOS の自動テキスト拡大（未指定＝既定 auto）＝ **拡大は止めず、レイアウトの側を耐えるようにした**（v70 で文字を大きくした要求と矛盾させない）。
> - 🔎 **同じ形を探しに行って2箇所目**＝1.5倍で **開始/終了の時刻欄（`f-startTime`/`f-endTime`）も画面外**に出ていた（スクショで「開始●」の右が空白なのはこれ）＝報告に無かったが同時に修正。
> - **直し**: `.sim` を**縦積み**（幅の取り合いを無くす＝文字が何倍でも溢れない。`min-width:0` の延命は「今の文字サイズなら収まる」だけ）／`.row` に **`flex-wrap: wrap`**（v48 と同じ手）／タイトルを **`<textarea rows=3>`**（store との配線は不変＝`el.value` と `activeElement`）／`simText` は **`rows=2`・Enter＝送るは据え置き・改行は Shift+Enter**。🚫 `overflow-x:hidden` で隠す手は採らない（**欄が見えなくなる**＝v45/v20）。🚫 CSS はテストで縛らない（守るのは「横スクロールしない」という**結果**であって `column` という**手段**ではない）＝代わりに**検査スクリプトを CHANGELOG に道具として残した**。
> - **実測**: 横溢れ **1.0/1.25/1.5/2.0倍すべて 0px**（修正前は1.5倍で +125px）／タイトル3行・発話欄2行が計算値で成立／**配線の無退行**（「明日15時に歯医者」→ 正しく入る・**store と画面が一致**・手入力→store・Enter/Shift+Enter）／通常サイズでは開始行は1行のまま（wrap の副作用なし）／console 0・BUILD=v76・**テスト 511/511**（別セッションが新設した `wiring.test` 8本も通過）。
> - 📌 **番号の経緯**: 当初 v75 として作ったが、**同じ日に別セッションが v75（来歴の ✏️）を先に push**していたため rebase して **v76 に繰り上げ**（触った場所は別・衝突は BUILD 行のみ・統合後に再検証済み）。
> - 🔑 **教訓＝レイアウトのバグは自分の画面では見えない**（既定フォントでは +2px しか溢れず目視で気づけない）＝**文字サイズを振って測る**。**アクセシビリティ設定は「他人の環境」ではなく、実際に使っている人の環境**。
> - ⚠ **見え方の最終確認はゆうの目**（この環境はプレビューが描画されずスクショが撮れない＝計算値で検証）。**Pages は即時／iOS は Codemagic 1.2 待ち／Android は次の AAB**。
>
> **🎨 v77（2026-08-04・ゆう要求）＝畳みを「カードの層」に＋下に固定のマイクバー**: Alexa のホーム画面を見せて「**いまはトグルを使っているけど、いまはやりの階層的な UI にするのはどうだろう**」。
> - **着手前に現状を測った**＝既定 1294px（1.6画面）・**主動線（マイク→フォーム→保存）は既に1画面**（保存の下端 605px < 812px）⇒ **長さが問題ではなく「同じ姿の畳みが8個並ぶ」のが弱点**と判明。参照画面を①カードの層 ②2列タイル ③下の固定入力バー に分解して選んでもらい、**①と③を採用**（「3つに束ねる」「下タブ」は不採用）。
> - 🚫 **主動線は触っていない**（階層化＝タップを足す＝背骨②「仲介者を消す」を削る方向）。変えたのは**下半分の畳み8個と、その外側だけ**。
> - **①**: 見出し（summary）をカードの**中**へ入れて「1枚のカード＝1つの機能」に。**開閉の仕組みは不変＝見た目だけ**。ダークは影が出ないので**縁の明るさ**で層を出す。見出しに絵文字1つ（既存の語彙のみ）。
> - **③**: **上のマイクが1pxでも見えている間は出さない**（同じボタンが2つ見える混乱を作らない）。🚨 **入口が増えるのと同時に道を1本にまとめた**＝ `toggleMic`/`cancelMic`（上下が同じ関数）・`renderMicState`（**録音中の見た目を触る唯一の場所**＝v76 までは2箇所に散っており「上は赤いのに下は待機中」が作れる形だった）・`syncDock`（表示判定は1つ・きっかけは IO と scroll の2つ・冪等）。
> - 🔴 **ハマり＝この環境では IntersectionObserver も scroll も一度も発火しない**（描画していないため。自前で作った observer・リスナで実測して**環境の話**だと切り分けた）。`syncDock()` を手で呼べば正しく切り替わる＝**判定は正しい／イベントが届くかだけ未検証**。
> - **テスト 515/515**（wiring +4）・**わざと4通り壊して全部落ちることを確認**（v10 の流儀）／実ブラウザ実測（カードの縁・バーが画面内に収まる・**録音中は上下が一斉に赤**・最下部でフッタが隠れない・**バーの実クリックが転写層まで到達**・v76 の横スクロール 0 を維持）・BUILD=v77。
> - ⚠ **実機で見る項目**（ここでは確認できない）: ①下へスクロールするとバーが出る ②上に戻ると消える ③バーから録音できる ④録音中に上下が同じ顔になる。
>
> **▶▶ その後＝Pages で v59 を触る（実機ビルド不要）＋溜まったら次へ**:
> 0. 🆕 **v59 の CSV**＝数日使って書き出し、**経路列でルール行 vs AI 行の訂正率**・**生テキスト×タイトル**（v58①）・**信頼度で認識/解釈の切り分け**が読めるか。読めれば v58② を「数」で判断できる。
> 1. 🆕 **v58② は測ってから**＝経路別の訂正率・「AI が記録を曖昧に倒す率」を見て、AI 契約に「記録で時刻なし→今に倒す・曖昧扱いにしない（ルールの v27 と揃える）」を教えるか判断（⚠「夕方あたり」を褒められた挙動を壊さない線を守る）。
> 2. 🆕 **App Store 新バージョン提出**（v32-v58 を公開版へ）＝やる時に段取り。素材は [docs/appstore.md](docs/appstore.md)。
> 3. 🆕 **v58① 閾値**＝実発話に合わなければ分割の数字（区切りあり 20字／区切り無し 40字）を下げる。
>
> **✅ 済（実機FB第33-35回で確認）**: v49「やめる」native ✓／v51 説明文 ✓／**v52 位置 native ✓**／v53/v54/v55-57 ✓／**App Store 公開 ✓**。
>
> **📮 App Store 1.0(12)**: ✅ **承認・リリース済み**（2026-07-22・Guideline 2.1 返信後）。lookup で裏取り＝[公開URL](https://apps.apple.com/jp/app/voicecalendar/id6791578087)。**次＝v32-v58 を載せた新バージョンの提出**（公開版は 1.0(12)＝≈v31 の早いビルド）。返信全文と宿題は [docs/appstore.md](docs/appstore.md) §11。
>
> **✅ 実装済み → v54（2026-07-22）**。以下は着手前の合意メモ（設計の出どころとして残す）。実装で加わったのは **カレンダーへの「改正volN」再追加**（実機FB第33回でゆうが出した答え＝write-only で消せないなら足して見分ける）と、**編集中は自動保存も掃除もしない**の2点。
> ~~**📋 次にやる = v52「保存済みの行を後から直す」（実機FB第30回の3つ目・設計はゆうと合意済み・着手は実機FB後）**~~
> 要求「**とりあえず保存して、あとからまとめて、リストや履歴で修正することがこれから増えそう。その時の手入力で変えたことも、辞書に登録して保存か、そのまま保存か選べるといい**」
> **合意した形＝「フォームに載せて直す」**（ゆう選択）:
> 1. リストの各行に **✏️** → その行の内容を**いつものフォームに載せる**（背骨①「フォームが単一の真実」＝新しい編集画面を作らない。**声でも直せる**のが効く）
> 2. 直す → 保存ボタンが「**更新して保存**」に変わり、**元の行を差し替える**（`records.js` に `update` を新設＝今は add/remove/clear のみ）
> 3. 手で直した欄があれば **v44 の「辞書に登録して保存」がそのまま出る**（基準＝読み込んだ時の値 → 直した値）＝要求の後半はこれで満たされる
> ⚠ **カレンダーに入れた分は直せない**（write-only＝読めない＝書き換えられない）。「両方」で保存していた行を直すと**リスト側だけが直り、カレンダー側は古いまま**＝ **toast で必ず明示する**（黙って部分成功にしない・v16）。
> ⚠ 編集中に音声が来た時の衝突（言い直し掃除 v6 / 欄ロック v3）と、編集を**やめる**導線（キャンセル）も設計に含める。
>
> **▶▶ 次回＝実機（TestFlight）でここを見る（v47/v48 のビルド後）**:
> - 🆕 **自動保存「いつでも」の手触り**（v47）＝「新宿駅に着いた」だけで入るか。⚠ **環境音・独り言で誤爆しないか**（v24 の自動録音と重なる場面が本番）。誤爆が出たら次の手＝「自動保存した直後に取り消す」導線（カレンダーからの削除が write-only で可能かは native 検証が要る）。
> - 🆕 **位置情報を「許可しない」で拒否したときの復帰**（v47・native 未検証）＝バナーが出て「設定を開く」で復帰できるか。
> - 🆕 **来歴の × 1件削除**が native でも同じか。
> - **CSV の共有シート**（v36）＝ native 初検証のまま（今回も未確認）。
>
> **▶▶ 旧「次回＝実機で見る」（v46 時点・上に置き換わった）**:
> - 🆕 **辞書の値を認識器へ（v44 の D・native のみ）**: 一度「今居→今井」を登録した後、**そもそも「今井」と認識される**ようになるか。⚠ **効きすぎ**（辞書の語に引っ張られて無関係な語が化ける）が起きないかも同時に見る。診断に `🎙 語彙ヒント 欄名=6 辞書=N` が出る。
> - **位置情報の許可ダイアログ文言**（v38・Info.plist）／**CSV の共有シート**（v36）＝どちらも native 初検証のまま
> - v43 の曖昧→メモ・v44/45 の辞書登録ボタンが **native でも同じに動くか**（web は確認済み）
> - **1.0 の審査結果**（この回の時点で lookup=0＝未公開のまま）
>
> **▶▶ 旧「次回はここから」（v43 時点のメモ・上に置き換わった）**: ① **v43 を実機（Pages / 次ビルド）で確認**＝「夕方あたり」がメモに残って保存されるか・手書きメモが潰れないか。② **モデルを `claude-sonnet-5` にして精度の体感**（1発話 ≈0.8円・Haiku ≈0.4円・Opus ≈2円＝**絶対額は2円以下**）。Opus は音声経路では 12秒に間に合わないことがある＝長文（まとめて入力・タイムアウト無し）向き。③ 未検証のまま残っている native 2つ＝**位置情報の許可ダイアログ文言（v38）**・**CSV の共有シート（v36）**。④ **1.0 の審査結果**（lookup=0＝まだ未公開）。
>
> **▶▶ 実機で試す（v36-v42・Pages でも／TestFlight 1.1 でも）**:
> - **v39 まとめて入力**: 「まとめて入力」パネル → 長文を貼る → 📄 AI用の指示をコピー → チャットAI（Claude 等）に貼る → 返った JSON を貼り直して取り込み → カードの ⚠（ambiguities/整形）が分かるか → ✏️ フォームで直す → すべて保存。焦点＝**iPhone Safari でコピーが動くか**（埋め込みブラウザでは activation 制限で失敗＝実タップなら通る想定）と、チャットAI往復の手触り。
> - **v40 BYOK**: AI 設定に**実キー**（Anthropic か Gemini・使用上限付き推奨）→ テスト送信 → 長文を貼って「🤖 AIで解釈して取り込み」。焦点＝**実キーでの CORS 疎通**（web=Pages は今日から・**native の capacitor://localhost origin は次 Codemagic ビルドで要確認**）と、実際の AI の読み取り精度（ambiguities の申告の質）。
> - **v42 音声→AI**: 詳細設定「音声を AI で解釈する」をオン（要キー）→ マイクで「来週の火曜の夕方あたりに本社で打ち合わせ」のような**ルールが解けない言い方**を実発話 → フォームに入るか・⚠の申告の質・**体感の待ち時間**（🤖AI解釈中…→数秒。12秒で時間切れ→ルールに戻る）。「明日10時会議、あと金曜ランチ」で**複数件→まとめて入力**も。精度が良ければ「AI 経路の自動保存」解禁を相談。
> - **v36 CSV**: リスト →「CSV書き出し」→ iPhone の**共有シート**で受け取れるか（native は次ビルド／web=Pages はダウンロード or コピー）。📅/📝トグルで対象件数が変わるか（WYSIWYG）。
> - **v37 辞書**: 「僕のメールアドレス」等を登録 →「メモ 僕のメールアドレス」と**実発話**して展開されるか（来歴に 🔤）。焦点＝**認識器が言い回し自体を正しく拾えるか**（外すなら Swift の contextualStrings に辞書キーを足す＝ビルド要）。短い言い回しの誤爆（2文字未満は登録拒否済み）も確認。
> - **v38 位置**: 詳細設定「保存時に位置情報も記録する」をオン → warm-up の**許可ダイアログ**が出るか → 保存先リストで「現在 ◯◯についた」→ 行の **🗺地図** で Apple/Google マップが正しい地点で開くか。実 GPS の取得時間の体感（保存はブロックされない設計）。native の許可文言（Info.plist）は次 Codemagic ビルドで。
>
> **▶▶ 次回はここから＝審査結果を待つ**:
> - Apple の審査（最大48h・メール通知）→ 通過なら（リリース設定次第で）公開／却下ならメールの理由に対応。
> - **公開の判定**は lookup API resultCount≥1 / 公開ページ 200 を実際に確認するまで主張しない（審査待ちの今は 0・404 で正常）。
> - 通過・公開後 → ノールック完走率の計測（SPEC §10・Phase 4）。
> - 提出フローの具体手順（素材は `docs/appstore.md`）＝下記（履歴）:
> - スクショ: iPhone 6.5型枠は投入済みか／**iPad 枠に 2048×2732 を投入**（別数字を要求されたら作り直す）。インストールシートは先頭3枚＝録音中を先頭に。
> - バージョン「1.0 提出準備」: 説明/サブタイトル/キーワード/プロモ/リリースノート（`docs/appstore.md` §1-5）・**サポートURL** `…/support.html`
> - アプリ全体: **プライバシーポリシーURL** `…/privacy.html`（「App のプライバシー」or「App 情報」・ゆう確認済み）・**App プライバシー=データ収集なし**（§7）・**App Review メモ**（§8）
> - → **「審査用に追加」**。通過後 → ノールック完走率の計測（SPEC §10・Phase 4）
> - 残（任意）: スクショにキャプション文字（訴求）／原本フル解像度で差し替え／英語ローカライズ
> ① **自動保存の手触り**（v28・詳細設定でオン）: 日時を言った発話だけ入るか／**雑音・独り言で勝手に入らないか**（v24 の自動録音と重なる場面が本番）／「自動保存しました」の toast で気づけるか
> ② 「今井さん」等の人名が「今」に誤爆しないか（v27 の誤爆ガードの実発話確認）
> ③ ここまで通ったら → **ノールック完走率の計測**（SPEC §10・Phase 4）＝道1を日常で使い、話しただけで正しく入った割合を数える
> ① **開いた瞬間に録音が始まるか**（v24・未検証: 診断の 🎙「自動録音（起動）」と赤いマイク）＋ホーム→再度開くで「復帰」も
> ② 保存先の行が **OS の既定（Google にしていれば `予定（Gmail）｜CalDAV`）** を映すか＋保存 toast の `→ ◯◯` が既定カレンダー名になるか
> ③ まだ見ていなければ: 権限ダイアログの文言（「追加のみ」か）・診断の `起動Nms`（v22「一度目だけミスる」の判定）
>
> **▶▶ その次＝Siri で v0 完結（Phase 3・ユーザー合意済み）**: App Intent 1つ（`openAppWhenRun`）＋ App Shortcuts で「予定入れて」。**「起動したら即録音」は v24 で完成済み**＝ Siri は「開く」までを担当すれば道1が通り **v0 が終わる**。
> - その後 → **ノールック完走率の計測**（SPEC §10・Phase 4）／拡張の判断材料（繰り返しは権限そのまま・タスク=リマインダーは full access が要る＝「追加のみ」の軽さを失う・誕生日は EventKit では書けない＝Contacts の話）
>
> **🖥 dev サーバの運用メモ（セッション1でハマった）**: PC にはマイク無し＝**実発話検証は iPhone（GitHub Pages）が主戦場**。PC ではテキスト欄（発話シミュレート）で試す。Claude の preview_start で起動したサーバは**ターン間で落ちる**（python プロセスごと消える→ブラウザは「接続できません」）。自分で触るときは自分のターミナルで `npm run serve`（root 配信・http://localhost:5275）。
>
> **✅ GitHub Pages 化（GO 済み・実施済み 2026-07-16）**: あの日と同じ「root に本体 → `scripts/sync-web.mjs` で www/ 生成（gitignore）」に組み替え。**web の編集は root の index.html / engine/ / input/ / adapters/**（www/ は生成物・手で触らない）。Capacitor へは `npm run cap:sync`（sync-web が先に走る）。

---

## Phase 1 — 一本道 (web)：録音 → 確定パーサ → フォーム → 保存 ✅（bootstrap で貫通）

- [x] 中立スキーマ + 共有状態ストア（fieldState / 欄ロック）＝ engine/schema.js
- [x] 確定的日本語日時パーサ（純関数・now 注入・LLMなし）＝ engine/parser.js + テスト35本
- [x] 単一フォーム（必須/任意の段差・任意は畳む）＋2経路配線 ＝ index.html
- [x] 編集中フィールドのロック（activeElement 由来の述語に一本化・スキップ通知）＋ テスト10本
      ※ v3 で実バグ修正済み（イベント基準と描画スキップの二重管理→ストアと画面がズレ、画面と違う値が保存され得た）。鉄則は CLAUDE.md 参照
- [x] 転写層（WebSpeech + simulate 注入口）＝ input/transcriber.js
- [x] 保存アダプタ境界（materialize + ics アダプタ）＝ adapters/calendar.js
- [x] GitHub Pages 配信（root 本体 + sync-web.mjs → www/ 生成）＝ iPhone Safari で実発話できる場
- [x] 実発話テスト第1回（iPhone・4発話）→「基本いける」＋外れ3系統を v5 で修正（昨日/一昨日・来週の◯曜・N後）
- [x] 来歴パネル（生テキスト・認識信頼度・解釈結果・素通し理由を端末内に30件）
- [x] 実発話テスト（iPhone 実機・計10回超の FB ラウンド）→ v5〜v17 で反映
- [x] **欄指定発話**（「終了22時」「場所 立川」「メモ ○○」＝その欄だけの差分・v17）
- [x] **「今」「現在」= 今の日時**（v27・誤爆ガード付き＝「今井さん」を壊さない）
- [x] **保存先の選択＋時系列リスト**（v32: カレンダー/リスト/両方・記録📝/予定📅は開始日時で自動判定・端末内台帳＝write-only のまま「見返す」を実現）
- [x] **パーサ補強 第1弾（v34）**: 漢数字の時刻（「午後三時」「十九時」・1〜6時の曖昧素通しは算用数字と同じ・「一時的」ガード）／「半」の分離（「10時 半」・「半分」誤読ガード）／相対時刻（「30分後」「2時間後」「1時間半後」・「後ろ倒し」を食わない・「明日 30分後」の矛盾は素通し）／範囲「今から◯時まで」（開始=今・終了=◯時）＝テスト25本追加
- [ ] パーサ補強（実データ駆動・続き）: 「夕方」だけ（時刻なし）/「今度の金曜」/ 欄指定の言い回しの揺れ、等、出てきたものから
- [ ] **👀 watch: 「場所は一度目だけミスる」（v22 で対処・要検証）** — 仮説＝セッションが冷えていて冒頭の「場所」が欠ける。対処＝load() でカテゴリ前倒し＋contextualStrings で欄名を教える。**次の実機で診断の 🎙「起動Nms」を見る**（初回だけ大きければ仮説が確定／変わらなければ別の原因＝来歴の 🗣 生テキストで切り分け）

## Phase 2 — iOS native 足回り

- [x] `npx cap add ios`（ios/ をコミット、spike と同流儀・SPM）
- [x] **転写のネイティブ化**: local-plugins/speech-recognition（SFSpeechRecognizer ja-JP・on-device 優先・無音1.8秒で自動確定/6秒で打ち切り・interim/final/state/error イベント）＋ transcriber.js の native 分岐（出口 API 不変＝解釈層は無変更）
- [x] **保存のネイティブ化**: local-plugins/calendar-events（EventKit・iOS17+ 書き込み専用アクセス）。**✅ 実機で成立（v23/v26）**
      ・v23: **権限拒否からの復帰導線を実際に配線**（`openSettings` は v11 から在ったが**誰も呼んでいないデッドコード**だった）
      ・v26: 保存先は **OS の既定カレンダー1本**（アプリ内選択は v23-v25 で作って実機で外した＝理由は Swift 冒頭。**作り直さない**）。「今どこへ入るか」の表示は残す
- [x] Info.plist 権限4つ／codemagic.yaml（あの日の ios-testflight 流用）
- [x] **Codemagic 初回ビルド → TestFlight 1.0 配信（2026-07-16・一発成功）**＝Swift プラグイン2本の初コンパイル通過
      ・あの日の資産が効いた: ASC キー `MadeleineASC`／Team 25TM5C27YT／codemagic.yaml は BUNDLE_ID だけ差し替え
      ・署名鍵は新規生成（リポ外 `Documents/voice-calendar-signing/cert_key`）→ Codemagic Secure env `CERTIFICATE_PRIVATE_KEY`（group `signing`）
      ・App ID + ASC App 作成済み（io.github.yutsutke.voicecalendar / SKU voice-calendar）・内部テスター（自分）登録済み
- [x] 輸出コンプライアンスを Info.plist で恒久回答（`ITSAppUsesNonExemptEncryption=false`・v12）＝毎ビルドの質問が消える
- [x] **実機で v0 の一本道を検証 → ✅ 完走（実機FB第16回・2026-07-17）**:
      ② ~~話し終わって1.8秒で自動確定~~ → **v16 で成立**（自動停止で声→フォーム）
      ③ ~~保存 → 本物のカレンダーアプリに入るか~~ → **v23 実機で成立**「iOS ネイティブカレンダーに書き込みできました」＝**EventKit 実走・v0 一本道の完成**
- [x] ~~Google カレンダーに出るか~~ → **道が確定（v18 FB）: iOS 設定でアカウント追加 → デフォルトカレンダーに指定**（アプリ内選択は不可＝v26 で撤去）
- [x] ~~診断の `識別子復元:OK/NG`~~ → **実機で NG と確定**（選んでも既定へ入る）→ v26 で機能ごと撤去＝この観察点は消滅
- [ ] **実機で残りの観察（v26 のビルドで）**:
      ① 権限の文言（**「追加のみ」の軽いダイアログだったか**＝iOS17 書込専用アクセスの検証・未報告）
      ② 診断の `起動Nms`（v22「場所は一度目だけミスる」の判定・未報告）
      ③ **開いた瞬間の録音**（v24・未実機）＝ 🎙「自動録音（起動/復帰）」
- [ ] 実機でノールック完走率を測り始める（SPEC §10）
- [ ] （v12 を実機に載せるには Codemagic で Start new build。手動トリガのみ＝あの日と同流儀）

## Phase 3 — Siri 起動（道1）✅ App Intent 不要で完成（v24+v29）

- [x] **「Hey Siri ボイカレ開いて」で開く** ＝ Info.plist の `INAlternativeAppNames`（v29・ボイカレ＋ボイスカレンダー）
- [x] **開いたら即録音** ＝ v24 の自動録音（起動/復帰・白紙の時だけ）
- [x] 話す → フォーム（v16）→ 保存（v23/v26）／設定オンなら**自動保存**（v28）＝**道1が Swift 追加コードゼロで貫通**
- ~~App Intent（openAppWhenRun）／App Shortcuts／UserDefaults バッファ~~ → **不要になった**。
  un-park する条件: 「ボイカレで**予定入れて**」のような**動詞フレーズ**を Siri に露出したくなった時だけ
  （その時は spike の「UserDefaults バッファ＋drain」パターンを App Intents 版に書き直して流用）

## Phase 4 — 計測・磨き

- [ ] ノールック完走率のローカル記録（保存後に「直しに戻ったか」を端末内でカウント）
- [ ] 曖昧パターン頻度の記録（notes の内容別カウント）→ v1 仮置き UI の投資判断材料

---

## 観察メモ（v0 検証で見る点）

- **👀 用途の発見（実機FB第19回）: 「メモアプリとしても使えそう」** — 予定を入れる道具が、**思いついたことを時系列に置く道具**にも見えている。v27 で「日時を言わなければ今」を入れて道は通した。**次に見るのは「実際にメモとして使うか」**（予定とメモの比率・メモが増えるとカレンダーが荒れないか）。荒れるなら OS 設定で**メモ専用のカレンダーを既定にする**だけで分離できる（v26 の「保存先は OS の既定1本」がそのまま効く＝アプリ側に何も足さずに済む可能性）。**用途が2つに割れてから可変機構を考える**（SPEC §12）。

- ~~**連続発話の混在状態**: 前の発話の日時が残る~~ → **実使用で問題化（FB第2回）→ v6 で解決**: 発話=言い直しセマンティクス（前回の音声欄は掃除・手入力は保護）。断片発話での「追加・修正」は引き続き v1 差分パッチの主戦場。
- **1〜6時の素通し**（「3時」→ 埋めない）が実際どのくらい起きるか。頻出なら v1 仮置き（15時と仮置き＋淡色）の最有力候補。
- **年の中間帯（v8→v9 で状況変化）**: 「3月1日」＝4か月前 or 8か月先、はどのルールでも当たらない（現在は「最も近い」＝今年3/1）。**v9 で「来年の3月1日」と言えばユーザー自身が解消できる**ようになった。→ 観察点は「**人は自然に『来年の』を付けるか、それとも黙って外した結果を手で直すか**」。前者なら v1 の `guessed`（仮置き UI）は不要かもしれない＝**作らずに済む可能性**。後者なら仮置きの最有力候補として un-park。
- Web Speech API は Chrome だとクラウド転写の可能性（開発用と割り切る。本命は SFSpeechRecognizer の on-device）。

## 今は作らない（SPEC §3 / un-park は検証◯＋明示要求が揃ってから）

- **複数欄を1発話で（「場所 立川 メモ 保険証」）＝実測で却下（v22）**: 欄名で分割すると「場所 **メモ**リアルホール」が「場所=空・メモ=リアルホール」に化ける（日本語は語境界が無い）＝ SPEC §7「創作しない」違反。**1発話1欄が正しい**（ユーザーの直感とも一致）。

- 道2（Siri 内完結）／確定・曖昧の自動振り分け
- 曖昧の仮置き UI（guessed 状態は schema に予約済み）
- 既存予定の読み取り・一覧・衝突検知
- 音声差分パッチ（「1時間後ろ倒し」）← v1 の主戦場
- 繰り返し / 参加者 / 添付・リッチ項目
- LLM 解釈・複数予定一括
- Android（CalendarContract アダプタの席だけ用意してある）
