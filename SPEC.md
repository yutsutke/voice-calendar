# 声で入れるカレンダー（仮称）— 要件定義 v0

> 音声と手が「同じ一つのフォーム」を同時に編集する、入力補助カレンダー。
> v0 は Apple 純正 / Google カレンダーへの **賢い入力面** として最小で出し、
> 育てた「入力エンジン」を後にライフログ（`plans`）へ移植する。

- プロダクト名：未定（仮称）
- リポジトリ仮称：`voice-calendar`
- 実装体制：Capacitor（「あの日」と同じ、単一の薄い層を想定）
- 初期対象：**iOS 先行**（Android は後述の理由で後追い）

---

## 0. 一行でいうと

「予定を書いて」と話すと、カレンダーのスキーマ項目が埋まったフォームが即座に立ち上がる。意図が明確なら**画面を見ずに保存まで完走**でき（ノールック）、任意項目を詰めたい時だけ画面を見て、声か手で足す。**音声と手入力は同じ一つのフォームを同時に更新する。**

---

## 1. 背景・経緯（なぜこの設計に至ったか）

この仕様は、一連の設計対話の結論として生まれた。判断の根拠を残すために系譜を圧縮して記す。

1. **出発点＝「OS×web が絡むと急に難しくなる」体感。** 既存アプリ（あの日）の開発で、カメラ・写真取り込み・バックグラウンドなど OS に接する領域で難度が跳ね上がることを経験。その難所の正体は個々の API ではなく **“幅”**（native+web+ブリッジ+権限+OS 差を同時に握ること）で、そこが個人開発の空白＝機会だと気づいた。AI が崩したのは「幅にかかる学習税」。
2. **AI ネイティブ再考。** 「チャットを玄関に貼る」は多くの場合設計ミス。NL は意図を言語化できる時に階層を畳めるが、GUI は「見て初めて気づく（recognition）」時に要る。最強は常に**ハイブリッド**。本質は表層の入れ替えではなく、**能力層（動詞＝できることを機械可読に）と表層（人が触れる面）を分離**し、GUI を数ある窓口の一つに降ろすこと。
3. **カレンダーで具体化。** 従来の音声入力は「言う→AI が解釈→“これでいい？”と確認→修正はまた言い直し」という**仲介者モデル**。これを捨て、**フォームを単一の真実（single source of truth）にし、音声と手をそこへの2本の直接入力経路にする**、という核を発明。確認ステップが消えるのが本質的な勝ち。
4. **流儀＝最小から始めて広げる。** まず Apple 純正 / Google カレンダーの**入力補助の単体アプリ**として出し、経験をライフログへ順次移植する。重要判断：**資産は「アプリ」ではなく「入力エンジン」**。宿主（純正カレンダー→やがて `plans`）から切り離せる形で作り、移植を「作り直し」でなく「差し替え」にする。
5. **起動＝ Siri から一言。** 二つの質の違うやり方がある。**道1**（Siri がアプリを開いて録音開始→自分のフォーム体験に着地）と、**道2**（Siri 内で保存まで完結＝真のノールックだが、フォーム＝心臓を捨てることになる）。**v0 は道1のみ。道2は反応を見てから。** 既存の `app-shortcuts` プラグインの蓄積が初速に直結する。
6. **Google 同期は OS に委譲。** OS のカレンダー層（EventKit）に書けば、OS が iCloud / Google への同期を持っていく。**アプリは Google と直接通信しない** → OAuth なし・バックエンドなし・Google 審査なし。純正用と Google 用を作り分けない（OS に書けば両方に届く）。

---

## 2. 設計思想（背骨・変更不可）

この6点は v0 以降も揺らさない。実装判断で迷ったらここに戻る。

- **フォームが単一の真実。** 音声と手はそこへの2経路にすぎず、どちらが書いても同じ1つの state を更新する。「音声の解釈結果」と「手入力の状態」を別々に持たない。
- **仲介者を消す。** AI は「あなたの代わりに入力する第二の指」であって、間に立って確認を求める存在ではない。確認ダイアログを出さない。
- **聞き返さない。** 曖昧は「聞き返す」でなく「**仮置き＋直せる**」に倒す。
- **ノールックが成立する条件を守る。** 画面を出さず声だけで完結してよいのは、**曖昧さがゼロで AI が黙って正しく1個に確定できる時だけ**。分岐がある時は勝手に創作しない。
- **ローカル完結を継承。** 音声認識・解釈は極力オンデバイス（あの日の思想を引き継ぐ）。外部送信は最小に。
- **入力エンジンは宿主非依存。** 中立スキーマ→薄いアダプタ、という境界を最初から引く（§6）。これが唯一の「先を見た先行投資」。

---

## 3. スコープ

### v0 で作る（これだけ）

- App Intent を1つ定義し、`openAppWhenRun = true` で **アプリを開いて録音開始**（道1）
- **App Shortcuts** で Siri フレーズを、ユーザー設定なしに最初から露出（`app-shortcuts` 資産を再利用）
- アプリ内**録音ボタン**でも同じフローを起動
- 音声→テキスト（**オンデバイス**優先）
- **確定的な日本語日時パーサ**（LLM なし）→ 中立スキーマへ
- **単一フォームへの2経路入力**（音声＋手）
- **編集中フィールドのロック**（人の手が触れている欄に AI は書き込まない）
- **EventKit へ保存**（Google/iCloud 同期は OS が担当）
- フォームは**必須/任意を視覚的に段差づけ**、任意は既定で畳む

### v0 で作らない（v1 以降の候補）

- Siri 内完結（道2）＝真のノールック
- 確定/曖昧の**自動振り分け**（確定は道2、曖昧は道1へ）
- **曖昧解釈の仮置き UI**（§7 の「推測つき」の可視化）
- **既存予定の読み取り**、一覧、衝突検知
- **音声による差分パッチ**（表示中の予定を「1時間後ろ倒し」等で修正）← v1 の主戦場
- 繰り返し / 参加者 / 添付 などリッチ項目
- パラメータ付き intent（目的語を Siri に解決させる）
- LLM による解釈
- 複数予定の一括入力
- Android 対応（道1なら素直に作れるが後述の理由で後）

---

## 4. 主要ユースケース

1. **ノールック確定入力（本命）**
   「明日15時に歯医者」→ タイトル=歯医者 / 日時=翌日15:00 がフォームに反映 → 画面を見ずに保存。完了。
2. **見ながら補正**
   声で骨格が入った後、画面を見て場所や通知など任意項目を手で追加・修正して保存。
3. **同時進行**
   音声で予定が入りつつ、その最中に場所欄を手で打っている → **手が触れている場所欄はロックされ、音声はそこを上書きしない**。
4. **Siri 一言起動（道1）**
   「Hey Siri、予定入れて」→ アプリが前面に出て録音開始 → 以降はユースケース1と同じ。

---

## 5. アーキテクチャ

層で分ける。各層は疎結合に保つ。背骨は **書き込みの入口は2つ（アクター別に最適化）／読み出しの描画は1つ**。

### 中核4層

1. **転写層（音声のみ）** — 音声 → テキスト。**生テキストを"来歴"として保存**する（オンデバイス転写）。この生テキストがあるから、解釈は後から再導出できる（→②）。
   - ルール：転写は来歴として紐づけるだけで、**自動で `note` 欄に流し込まない**（カレンダーが散らかる）。全文をメモに入れるのは opt-in。
2. **スキーマ拘束・解釈層（音声のみ）** — `(transcript, schema)` → `DraftEvent` への **patch**。**副作用のない純関数**にする＝同じ転写から何度でも欄を再導出できる。
   - v0 は **確定的な日本語日時パーサ**のみ（LLM を通さない）。
   - 「自由な合成」ではなく **選択＋充填**：新しいウィジェットを発明せず、既知スキーマの中で"どの欄を埋めるか"を決めるだけ。→ 型検証がタダで付き、フォームが常に見慣れた形＝**オリエンテーション摩擦が出ない**。
   - **逃げ道（freeform sink）必須**：スキーマ外の発話（「Emma に買った物を持っていく」等）を受ける `note` を1つ用意し、拘束を拘束衣にしない。
3. **共有状態層** — `DraftEvent` = **single source of truth（単一の真実）**。2つの入口がこの1つの state を更新し、1つの描画へ戻す。**欄ロック＝衝突ポリシーはここに宿る**（→§8）。
4. **二入口・単一描画** — 人間と AI はエルゴノミクスが逆なので、入口を分けるのは"便利"でなく**必然**。
   - **入口A（人間）**：描画された面の**直接操作**（タップ / ピッカー / キーボード）。
   - **入口B（音声＝AI）**：**型付きの構造化パッチ**（検証済み `DraftEvent` の差分を `setState`）。AI に人間用テキスト欄を打たせたり擬似ウィジェットをクリックさせない（不器用な "computer use" は損失）。
   - **描画（1つ）**：共有状態 → フォーム。**AI が書いた結果が常に見える**＝見て直せる。だから「ノールック」が"盲目"でなく"任意"になる。

### 前後の層（v0 から必要）

- **起動層** — App Intents ＋ App Shortcuts。v0 は `openAppWhenRun = true` の1 intent のみ（道1）。
- **永続層** — `DraftEvent` → **薄いアダプタ** → `EventKit`（iOS）。（将来）`CalendarContract`（Android）。Google / iCloud 同期は OS が担当。アダプタを差し替えるだけで宛先を変えられる（→§6）。

### v0 と "生成" の線引き（正直に）

予定のスキーマが固定（title / start / end / location / note / allDay）なら **フォームは1種類**で、AI はそれを埋めるだけ＝**UI 生成は不要**。「スキーマ拘束の"生成"」が効くのは**スキーマが可変になった時**（飛行機予約 vs 夕食 vs 定例授業＝別サブフォーム）。したがって：

- **v0** ＝ 固定フォーム＋充填（"どの欄を埋めるか"の選択のみ）。
- **v1** ＝ 種類が増えたら "**どのフォーム／サブフォームを出すか**" を選ぶ生成層を足す。

多様性が出る前に可変機構を作らない（§9「LLM は v0 で入れない」と同じ精神）。

### フロー

```
【書き込み：入口は2つ・アクター別】
  入口A 人間 ── 面を直接操作(タップ/ピッカー/キーボード) ─────────────┐
  入口B 音声 ─→ ①転写(生テキスト=来歴)                              │
                └→ ②解釈(スキーマ拘束: 選択+充填) ─ patch ──────────┤
                                                                    ▼
【共有状態】     ③ DraftEvent = 単一の真実 (欄ロック=衝突ポリシー §8)
                                                                    │
【読み出し：描画は1つ】  ③ → フォーム描画 (AIの結果が見える=見て直せる)
                                                                    │
【永続化】       DraftEvent → アダプタ → EventKit ─(OSが同期)→ iCloud / Google
```

---

## 6. 中立スキーマ（移植の要）

エンジンが吐く中間表現を **EventKit 固有にせず、素の中立形**で持つ。ここから各宛先へ変換する薄いアダプタを1枚挟む。**この1枚が、単体 → ライフログ移植のコストをほぼ消す。**

3つに分けて持つのが今回の肝：**① 構造化結果（`DraftEvent`）／② 各欄の状態（描画・ロック用）／③ 来歴（`transcript` への参照）**。②③ を結果本体から分離する。

```ts
// ① 中立スキーマ（v0）— 宿主非依存の中間表現
type DraftEvent = {
  title: string;
  start: Date | null;
  end: Date | null;
  location: string | null;
  note: string | null;                     // freeform sink: スキーマ外の発話の受け皿（§5-②）
  allDay: boolean;
  fieldState: Record<Field, FieldState>;   // ② 各欄の状態
  source: Provenance;                       // ③ 来歴（note には流し込まない）
};

type Field = 'title' | 'start' | 'end' | 'location' | 'note' | 'allDay';

// ② 欄ごとの状態：描画の“仮”表示（§7）と 欄ロック（§8）に使う
type FieldState =
  | 'empty'        // 誰も触れていない → 空欄のまま（AIは創作しない）
  | 'guessed'      // AIが推測で充填 → 描画で“仮”＝淡色/点線（v1）
  | 'confirmed'    // 発話／手入力から確定
  | 'locked';      // 人が編集中 → AIはこの欄に書かない（§8）

// ③ 来歴：生テキストを別に保持し、(transcript, schema) から欄を再導出できる純関数にする
type Provenance = {
  transcriptId: string | null;
  parser: 'deterministic' | 'llm' | null;  // 誰が解釈したか（v1で 'llm'）
};

type Transcript = {
  id: string;
  text: string;             // 音声の生テキスト（オンデバイス転写, §5-①）
  createdAt: Date;
};

// 解釈は副作用のない純関数（§5-②）＝録り直さず再導出できる:
//   interpret(transcript: Transcript, schema): Partial<DraftEvent>   // 選択＋充填
```

- **アダプタ（v0）**：`DraftEvent → EKEvent`。`fieldState` / `source` / `Transcript` は **EventKit へ渡さず端末内に留める**（＝ローカル完結）。
- **移植時**：同じ `DraftEvent` を **ライフログ `plans.stops` 向けアダプタ**に差し替える。`stops` の要素は `{ name, lat, lon, start, end, memo, budget, photos }` ＋ **安定 uid**（別途設計済み）で中立スキーマとほぼ同形（`note`↔`memo`、`location`↔`name/lat/lon`）。Google 同期の対応表は本体に混ぜず `gcal_map` / `gcal_state` に隔離する既存方針に接続する。
- **来歴が効く場面**：v1 で LLM を足した時、過去の `Transcript` から欄を**録り直さず再導出**できる。パースが「金曜」を誤って割り当てても、生テキストが接地点として残る。
- つまり **「1件の予定を音声で入れる」の洗練が、そのまま「旅程の1 stop を音声で入れる」の予行**になる。

---

## 7. 曖昧さの三段階

全部を同じに扱うと壊れる。モデルは3段階。**v0 は「確定」だけを正面から実装**し、それ以外は素通し（仮置き UI は v1）。

| 段階 | 例 | 扱い | v0 |
|---|---|---|---|
| **確定** | 「明日15時に歯医者」 | ノールックで即フォームに反映 | ◯ 実装 |
| **推測つき** | 「金曜ランチ」→ 12:00 と仮置き | 反映するが**その項目だけ“仮”と分かる印**（淡色/点線）。**聞き返さない** | △ v1（v0 は仮置きせず素通し） |
| **空** | 終了時刻を誰も言っていない | 空欄 or 既定値。**AI は創作しない** | ◯（空欄のまま） |

v0 の実挙動：確定文は正しく入る。曖昧文は無理に解釈せず、該当項目を空のまま残してユーザーに委ねる。

---

## 8. コンフリクト解決（音声 × 手）

「同時進行で反映」を成立させる中核。ここの手触りで信頼が決まる。

- 原則：**人間の手が能動的に編集中のフィールドはロックし、AI はそこを書かない。**
- 「最後に触った経路が勝つ」ではなく「**編集中の経路を保護**する」。
- ロックはフィールド単位（例：場所欄を打っている間、音声は日時・タイトルには書けるが場所には書けない）。
- フォーカスが外れたらロック解除。

---

## 9. 技術選定（判断の根拠つき）

- **音声認識**：iOS `SFSpeechRecognizer`（on-device 対応）を優先。低遅延＆ローカル完結に合う。ローカル Whisper は重ければ後回し。
- **日時パース**：`chrono` 系 ＋ 日本語ルール（"明日" "来週火曜" "月末" "15時" 等）を自前で補強。**確定的**なので速く・安く・外れにくい。
- **LLM**：**v0 では入れない。** 理由は3つ ——(a) 遅延とコスト、(b) API 送信でローカル完結が崩れる、(c) 非決定性がノールックの信頼を毀損。**確定パーサで解けない曖昧・自由文が実際に頻出すると分かってから**、曖昧部分だけに足す。
- **保存**：`EventKit`。Google 同期は OS に委譲（OAuth・バックエンドなし）。ユーザーが OS でカレンダーアカウントを繋いでいれば、書き込み先に Google カレンダーを選べる。
- **起動**：App Intents / App Shortcuts。既存 `app-shortcuts` プラグインの資産を再利用（ゼロからの Siri 連携ではない）。
- **プラットフォーム**：**iOS 先行**。Android は道1（アプリ起動）なら素直だが、Siri 内完結の等価（App Actions / Assistant intents）は仕様が不安定で薄いため後回し。日本の BtoC は iOS 優先が定石という点とも一致。

---

## 10. 検証指標（最重要）

測るのは「便利そうか」ではなく **ノールック完走率** ——
**話しただけで画面を見ずに正しく入り、直しに戻らなかった割合。**

- 高い → 核が当たっている。
- 低い → 原因を切り分ける：①音声認識の精度か ②日時解釈の精度か ③そもそも人は不安で画面を見てしまうのか。
- 道2（Siri 内完結）を将来足したら、指標を **(a) 道1 完走率 / (b) 道2 完走率** に割り、**v1 の投資先の判断材料**にする（道2 が低ければ「真のノールックは案外要らない＝道1で十分」という学び）。

---

## 11. ロードマップ

- **v0**：道1のみ／確定入力／2経路入力＋編集中ロック／EventKit 保存／iOS。
- **v1 候補**：
  - 曖昧の「推測つき」仮置き UI（§7）
  - 既存予定の読み取り ＋ **音声差分パッチ**（表示中の予定を一言で修正）← 真価はここ
  - Siri 内完結（道2）＋ 確定/曖昧の自動振り分け
  - 繰り返し / 参加者 など
- **移植フェーズ**：中立スキーマのアダプタを `plans.stops` 向けに差し替え → ライフログへ音声旅程入力。`gcal_map` / `gcal_state` 隔離・stop 安定 uid の既存設計と接続。

---

## 12. リスク・落とし穴

- **道2の誘惑**：Siri 内完結は魅力的に見えるが、フォーム＝単一の真実（＝この製品の心臓）を捨てることになる。**v0 では触らない。**
- **フォームのプレッシャー**：常時見えると「全部埋めなきゃ」と感じさせる。必須/任意を段差づけ、任意は畳む。“見なくてよさ”がノールックの前提。
- **日本語日時の曖昧（"金曜"問題）**：安易に LLM へ逃げず、まず素通しで実データを測る。
- **EventKit 権限フロー**：写真プラグインで経験済みだが、拒否・限定許可の分岐は丁寧に。
- **移植を見据えた中立スキーマは最初にやる**：後付けは高い。§6 の境界を初日に引く。

---

## 13. v1 拡張: まとめて入力（un-park 記録・2026-07-19）

§3「v0 で作らない」の **「LLM による解釈」「複数予定の一括入力」を un-park** した。条件は成立している＝ユーザーの明示要求（①長文で複数項目を一発入力 ②AI の API で解釈レベルを上げる ③AI が読み書きできるスキーマを最初から用意）＋ v0 の検証完了（一本道の実機完走・App Store 提出）。**§2 の背骨は変えない**——以下がその整合。

- **音声の短文経路は不変**: ルールベース（オンデバイス・確定的）のまま。AI は**別入口**（長文バッチ）の opt-in＝エンジンは統合しない。§9 の LLM 三理由（遅延・外部送信・非決定性）は「短文の音声経路に入れない」根拠として今も正しい。
- **契約が単一の真実**: `engine/contract.js` の JSON Schema（description は日本語の意味的制約）が、AI へのプロンプト・WebMCP の inputSchema・検証ゲートの3箇所で**同じ現物**として使われる（二重管理ゼロ）。provenance は §6 で予約済みの `parser: 'llm'` の席に収まる。
- **確認ダイアログを出さない（§2-2）のまま**: バッチの受け皿は「取り込みリスト」＝**編集可能な作業面**（モーダルではない）。編集は常に唯一のフォーム（§2-1）へ載せて行い、保存は明示ボタン。**どの入口からも直接保存しない**——バッチは §2-4「曖昧さゼロ」を機械的に保証できないため、自動保存（v28）とは接続しない。AI が確信を持てない点は契約の `ambiguities` で申告させて**見せる**（聞き返さない §2-3 の路線＝仮置きでなく明示）。
- **ローカル完結（§2-5）の扱い**: 既定は今までどおり外部送信ゼロ。AI 利用は **BYOK（ユーザー自身のキー・opt-in）**＝本文はユーザーが選んだ AI 社へ**直接**送る。開発者のサーバは存在しない（無料公開・運用者の課金リスクなし）。
- 実装の刻み: v39 契約＋検証ゲート＋取り込みリスト＋JSON 貼り付け＋AI用指示コピー（キー無しで動く）／ v40 BYOK（Anthropic・Gemini）／ v41 WebMCP 登録。

---

### 付記（Claude Code への渡し方）

本ファイルはそのまま新規リポジトリの spec として置ける。運用は既存流儀に合わせ、この内容を種に `CLAUDE.md`（リポジトリ直下）＋ `CHANGELOG.md` / `TODO.md` の三層に展開するとよい。最初の実装の切り口は **§3「v0 で作る」の上から順**。まず「録音ボタン → 確定パーサ → フォーム → EventKit 保存」の一本道を通し、その後に App Intents / App Shortcuts で Siri 起動（道1）を載せる。
