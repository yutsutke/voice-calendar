// engine/settings.js — 詳細設定（v19）
//
// **設定を足す基準**（SPEC §12「多様性が出る前に可変機構を作らない」の運用）:
//   実機FBで **実際に迷い・事故が起きた決定** だけを設定にする。「あると便利そう」は入れない。
//   既定値は必ず **これまでの実挙動**（＝設定を触らない人の体験は1ミリも変わらない）。
//
// 保存は localStorage（端末内・外部送信しない＝ローカル完結 SPEC §2）。
// engine 層に置くが DOM も宿主も知らない（純粋な値の入れ物）＝ライフログ移植時もそのまま持てる。
(function (global) {
  'use strict';

  const KEY = 'vc_settings_v1';

  // 各設定: 既定値・ラベル・説明・根拠（なぜ設定になったか＝将来「これ要る？」の判断材料）
  const DEFS = [
    {
      key: 'recordDest',
      label: '保存先',
      hint: 'カレンダー: OS の既定カレンダーへ（今までの動き）。リスト: カレンダーには入れず、アプリ内の「リスト」に時系列で残す（この端末の中だけ・同期なし）。両方: 両方へ入れる＝記録（開始が今〜過去）はカレンダー側に 📝 が付く。※アプリはカレンダーを読めないため、カレンダーに入れた分を消すのはカレンダーアプリで。',
      why: 'v32 ユーザー明示要求（2026-07-18）: v27 でメモ用途が成立した先の実問題＝記録が増えるとカレンダーが予定でいっぱいになる。メモ的な記録をカレンダーに入れずリストへ逃がす道（SPEC §12「用途が2つに割れてから可変機構を作る」の“割れた”局面）',
      def: 'calendar',
      type: 'enum',
      options: [
        { value: 'calendar', label: 'カレンダーのみ' },
        { value: 'list', label: 'リストのみ' },
        { value: 'both', label: '両方' },
      ],
    },
    {
      key: 'captureLocation',
      label: '保存時に位置情報も記録する',
      hint: 'オン: 保存した時にいた場所（緯度経度）をリストの行に残し、🗺地図 で開ける。取得は保存を待たせない（取れた時だけ後から行に付く）。リスト/両方で保存した行だけが対象（カレンダーのみの保存には付かない＝アプリはカレンダーを読めないため）。位置はこの端末の中にだけ保存され、外部送信されない。オフ: 何もしない（今までの動き）。',
      why: 'v38 ユーザー明示要求（2026-07-19）「データ保存の際に現在の位置情報を取得。アップルマップや、グーグルマップと連携して、記録した地点で表示できるようにしたい」。位置情報は敏感＝黙って取らない＝既定オフ（オンにした人だけ動く。v19 の既定不変原則＋プライバシーの opt-in）',
      def: false,
    },
    {
      key: 'plainUtteranceIsNew',
      label: '欄名なしの発話は「新規」にする',
      hint: 'オン: 前の音声入力を消して新しい予定として入れ直す（今までの動き）。オフ: 消さずに足すだけ。',
      why: 'v18 FB「意図せず新規になるときがある」。人によって・場面によって望む挙動が違うと分かった',
      def: true,
    },
    {
      key: 'protectManualEdits',
      label: '手入力した欄は音声で消さない',
      hint: 'オン: 手で打った場所やメモは、新規の発話でも残る（今までの動き）。オフ: 音声の欄と同じように消える。',
      why: 'v6 で「手入力は労力なので保護」と決めた判断。FB で明示的に選択したいと要求された',
      def: true,
    },
    {
      key: 'lockEditingField',
      label: '編集中の欄には音声を書き込まない',
      hint: 'オン: 打っている最中の欄は音声から守る（今までの動き）。オフ: 音声が上書きする。',
      why: 'SPEC §8 の核。オフにする人はまず居ないが、v3 の実バグの温床だった箇所なので切り分け用に露出',
      def: true,
    },
    {
      key: 'autoOpenOptional',
      label: '任意の項目に入ったら自動で開く',
      hint: 'オン: 音声が終了・場所・メモに書いたら畳みを開いて見せる（今までの動き）。オフ: 畳んだまま。',
      why: 'SPEC §12「フォームのプレッシャー」＝常に見えると全部埋めたくなる。ノールック派は閉じたままが良い',
      def: true,
    },
    {
      key: 'autoSaveAfterUtterance',
      label: '話し終わったら自動で保存する',
      hint: 'しない: 保存を押す（今までの動き）。日時を言った発話だけ: 「明日15時に歯医者」「現在 牛乳買う」のように日時を言った発話が、保存を押さなくても入る。いつでも: 日時を言わなかった発話（「新宿駅に着いた」）も、今の日時で入る＝メモ用途で「現在」と言わなくてよくなる。※ どの設定でも、曖昧な発話・欄名で始まる発話・言い淀みだけの発話（「えーっと」）は自動保存しない（フォームに残るので保存を押す）。',
      why: 'v28 FB「記録後、保存を自動にできる設定」＝メモ用途（v27）だと「話す→保存を押す」の1タップが冗長＝ノールックの究極形。既定は「しない」: **保存は不可逆**（アプリに読み取り権限が無い＝入った予定はカレンダー側で手で消すしかない）＝誤認識がそのまま残る。だから「曖昧さゼロで黙って1個に確定できる時だけ」（SPEC §2-4）に限る。／**v47 で boolean → 3択に拡張**（実機FB第29回「現在と言うのがだんだん面倒。時間の記録がないときは現在の時間で自動保存してほしい」）。「いつでも」は **v28 で意図的に塞いだ穴を開ける**選択肢（パーサは意味を判定しない＝雑音もタイトルになり v27「日時なし→今」で確定し、v24「起動＝即録音」と重なると開くだけで環境音が予定になる）＝(a) 選んだ人だけが引き受ける (b) 言い淀みだけの発話は全モードで弾く（VCParser.isFillerOnly）(c) 保存先を「リスト」にしていれば1件削除できる、の3枚で受ける',
      def: 'off', // ← 既定＝今までの動き（触らない人の体験は変わらない）
      type: 'enum',
      options: [
        { value: 'off', label: 'しない（保存を押す）' },
        { value: 'datetime', label: '日時を言った発話だけ' },
        { value: 'always', label: 'いつでも（日時なしは今の日時）' },
      ],
      // v46 まで boolean だった保存値の移行。🚨 これが無いと enum の検証（options に無い値は既定へ）に
      // 落ちて **使っていた人の自動保存が黙って off に戻る**。true の当時の実挙動＝「日時を言った発話だけ」。
      migrate: (v) => (typeof v === 'boolean' ? (v ? 'datetime' : 'off') : v),
    },
    {
      key: 'voiceAI',
      label: '音声を AI で解釈する',
      hint: '🤖 この機能には自分の API キーが要ります（コピーの往復を省いて AI に直接解釈させるため）。キーはこの「詳細設定」の下のほうにある「AI 設定」で登録します。キーが無いと、オンにしても今までのルール解釈のままです。／オン: 欄名で始まらない発話を、登録した API キーで解釈する。長く自然な言い方に強くなり、複数の予定を読み取った時は「まとめて入力」のリストに入る。発話のたびに API を1回呼ぶ（少額の従量課金・応答まで数秒）。AI が失敗した時はいつものルール解釈に自動で戻る。オフ: 今までのルール解釈（オフライン・即時・無料）。※ AI で解釈した発話を自動保存するかは、下の「AI で解釈した発話も自動保存する」で選べます。',
      why: 'v42 ユーザー明示要求（実機FB第24回・2026-07-19）「音声入力→AIエンジン適応からの入力のパイプラインもつくりたい。詳細で ON/OFF できるようにしたい」。既定オフ＝音声の核経路はオフライン・決定的のまま（SPEC §2-5/§9）。AI は言っていない日時を創作し得る＝「日時が在る＝実際に言った」というルールパーサの保証が無いため、自動保存(v28)の対象にしない（ユーザー合意済み・実機FBで精度を見てから再検討）',
      def: false,
    },
    {
      key: 'autoSaveAI',
      label: 'AI で解釈した発話も自動保存する',
      hint: 'オン: 「音声を AI で解釈する」で読み取った発話も、上の「話し終わったら自動で保存する」の設定に従う。オフ: AI で解釈した発話はフォームに残る（保存を押す）＝今までの動き。※ どちらでも、AI が「曖昧だ」と申告した発話（メモ欄に ⚠ が入るもの）は自動保存しない。',
      why: 'v47 ユーザー明示要求（実機FB第30回・2026-07-21）「AI使用時、自動保存ON/OFFをつくりたい。ONのときは、話終わったら自動で保存するに従う」。**v42 では意図的に塞いでいた**（AI は創作し得る＝ルールパーサの「patch に日時が在る＝実際に言った」という保証が無い／保存は不可逆）が、その時に「実機FBで精度を見てから再検討」と条件を明記していた → 実機FB第25回「かなりいい感じ。**あいまいなものを確定させてないのはいい**」＝ SPEC §7 が AI 経路でも保たれていると実測で確認できた＝**un-park の条件が揃った**。既定オフ＝v42-v46 の挙動は不変。安全弁は shouldAutoSave の `notes` の門＝AI の ambiguities/problems がそのまま notes に入る＝**AI が少しでも曖昧だと言った発話は自動保存されない**',
      def: false,
    },
    {
      key: 'autoSaveBatch',
      label: 'まとめて入力の取り込みを自動で保存する',
      hint: 'オン: 「まとめて入力」に取り込んだ予定を、カードを確認しなくてもそのまま保存する。※ AI が「曖昧だ」と申告した予定・項目を無視した予定・一部を取り込めなかった回は自動保存せず、カードに残ります（確認して保存を押す）。※ 外部のアプリや AI エージェントからの取り込み（WebMCP）は、この設定に関わらず必ずカードに残ります。オフ: 取り込んだ予定はカードに並び、「すべて保存」を押すまで保存されません（今までの動き）。',
      why: 'v53 ユーザー明示要求（実機FB第33回・2026-07-22）「音声解釈の精度が高いので、AI におけるまとめて入力に自動保存ありにしましょう」。**v39 では「どの入口からも直接保存しない」と決めていた**（取り込みリスト＝人が確認する関門）が、根拠は「AI の出力を信用しない」＝精度が未知だったこと。実機FB第25回「v40 長文＝完璧にできました」／第31回「曖昧を申告した発話はフォームに残る＝設計どおり」で**精度と申告の質が実測できた**＝ v42→v48 と同じ un-park の形。既定オフ＝触らない人の体験は不変。安全弁は3枚＝(a) AI 自身の ambiguities 申告 (b) 検証ゲートが項目を無視した problems (c) 封筒の warnings（落とした/切り捨てた回は**まるごと**人が見る＝信用の単位は「回」）。**WebMCP は対象外**＝外部エージェントが saveGate を素通りできてはいけない（v41 で明記した線・背骨②はエージェント相手では破れない）',
      def: false,
    },
    {
      key: 'autoSummarize',
      label: '下書きで、話がたまったら自動で要約する',
      hint: 'オン: 下書き（推敲）画面で続きを声で足していくと、前回 AI を当ててから話した分が 100字 を超えたところで自動的に「要約する」が走る。オフ: 自分で「📝 要約する」を押した時だけ走る（今までの動き）。※ 走ったことは画面に出て、↩ で元に戻せます。手で打っている最中には走りません。',
      why: 'v94 ユーザー明示要求（2026-08-30）「下書きボタンに☑みたいのを場所を入れて、ある程度しゃべると、自動に要約がはしる設定も欲しい」。**AI が自動で本文を書き換える**＝v80 で引いた「黙って書き換えない」といちばん緊張する所なので、(a) 既定オフ (b) ☑を押した人だけ (c) 走ったら必ず理由を出す (d) ↩ で1段戻せる (e) 走るのは**声で足した直後だけ**（手入力中は走らない）(f) 数えるのは「前回 AI を当ててから足した分」＝**要約の要約**が延々と走らない、の6枚で受ける。UI は下書き画面の ☑ **1つだけ**（詳細設定には出さない＝同じ設定の入れ物を2つ作らない・v93/v74）',
      def: false,
      render: 'custom', // 詳細設定の汎用ループでは描かない＝この設定の UI は推敲画面の ☑ ただ1つ
    },
    {
      key: 'defaultDurationMin',
      label: '終了を言わなかった時の長さ',
      hint: '開始だけ言った予定に、この長さの終了時刻を付けて保存する。',
      why: 'v1 でアダプタに +1時間を決め打ちした（SPEC §7「AIは創作しない」との緊張点）。人により 30分/2時間',
      def: 60,
      type: 'number',
      options: [15, 30, 60, 90, 120],
      fmt: (v) => `${v}分`,
    },
    {
      key: 'silenceMs',
      label: '話し終わってから確定するまで',
      hint: '無音がこの長さ続いたら録音を止めて確定する。短い＝速いが言い淀むと切れる。長い＝待たされる。',
      why: 'v15/v16 で 1.8 秒と決め打ち。「長すぎ＝待たされる／短すぎ＝切られる」は実機でしか分からないと明言していた',
      def: 1800,
      type: 'number',
      options: [1200, 1800, 2500, 4000],
      fmt: (v) => `${(v / 1000).toFixed(1)}秒`,
      native: true, // native の音声プラグインに渡す（web では効かない）
    },
  ];
  // targetCalendarId は **v26（iOS）で撤去 → v68（Android）で復活**させた。同じ名前だが前提が違う:
  //   iOS: write-only ＝アプリはカレンダー一覧を読めず、選択を次の起動へ持ち越せない（実機FB第17回）
  //        → 設定に残せば「選べるのに効かない」嘘になる。正しい道は OS 設定 → デフォルトカレンダー。
  //   Android: **READ_CALENDAR を恒久的に持っている**（保存先の解決に要る）＝一覧を読め、id を保存して
  //        次回も解決できる。**そして OS 側に「新規イベントの既定カレンダー」設定が無い**。
  // ＝ v26 の判断を否定したのではなく、**制約が違うプラットフォームで別の答えを出した**。
  DEFS.push({
    key: 'targetCalendarId',
    label: '保存先カレンダー',
    hint: '予定を書き込むカレンダーを選びます。「自動」は端末の主カレンダーに入れますが、Google アカウントが2つ以上あると「主」も2つあるため、どちらに入るかは選べません（アプリは今どこへ入るかを下に表示します）。',
    why: 'v68 実機FB第37回（2026-07-24・Android）: 保存は成功しているのに Google カレンダーに出ない → 診断で判明した真因は **端末に Google アカウントが2つ（yahoo と gmail）在り、どちらも IS_PRIMARY=1 で同点** ＝ 自動選択は「先に見つかった方」＝実質「先に端末へ追加された方」を選んでいた（ユーザーから見れば恣意的）。ゆうが見ていたのは gmail、アプリが書いていたのは yahoo。**iOS には OS の「新規イベントの既定カレンダー」があるので v26 でそれに乗れたが、Android にその設定は無い**＝機械的に1本へ decide できるのは候補が1本の時だけ。v19 の基準（実機で実際に事故が起きた決定だけを設定にする）を満たす',
    def: '', // 空＝自動選択（**v65-v67 の実挙動そのまま**＝触らない人の体験は変わらない）
    type: 'id', // 取りうる値が実行時に決まる（端末のカレンダー一覧）＝固定の options を持てない
    render: 'custom', // 一覧を native から取るため、詳細設定の汎用ループでは描かない（index.html が描く）
  });

  function load() {
    const out = {};
    for (const d of DEFS) out[d.key] = d.def;
    try {
      const saved = JSON.parse(global.localStorage.getItem(KEY));
      if (saved && typeof saved === 'object') {
        for (const d of DEFS) {
          const raw = saved[d.key];
          if (raw === undefined) continue;
          // 型が変わった設定の移行（v47）。壊れた値は migrate がそのまま返し、下の検証が既定へ落とす
          const v = d.migrate ? d.migrate(raw) : raw;
          if (d.type === 'number' && typeof v === 'number') out[d.key] = v;
          else if (d.type === 'enum' && (d.options || []).some((o) => o.value === v)) out[d.key] = v; // 選択肢に無い値は既定のまま
          // 'id'（v68）: 取りうる値が実行時に決まる＝ここでは**文字列であること**しか検証できない。
          // 実在するかは宿主が確かめる（無ければ native が TARGET_NOT_FOUND を返す＝黙って別へ倒さない）
          else if (d.type === 'id' && typeof v === 'string') out[d.key] = v;
          else if (!d.type && typeof v === 'boolean') out[d.key] = v;
        }
      }
    } catch { /* 壊れた保存値は既定にフォールバック（黙って壊れない＝既定で動く） */ }
    return out;
  }

  function createSettings() {
    let values = load();
    const listeners = new Set();
    return {
      DEFS,
      get: (key) => values[key],
      all: () => ({ ...values }),
      set(key, value) {
        const d = DEFS.find((x) => x.key === key);
        if (!d) return;
        if (d.type === 'enum' && !(d.options || []).some((o) => o.value === value)) return; // 未知の値で壊さない
        if (d.type === 'id' && typeof value !== 'string') return; // 文字列以外は受けない（実在確認は宿主）
        values[key] = value;
        try { global.localStorage.setItem(KEY, JSON.stringify(values)); } catch {}
        listeners.forEach((fn) => fn(key, value));
      },
      resetAll() {
        values = {};
        for (const d of DEFS) values[d.key] = d.def;
        try { global.localStorage.removeItem(KEY); } catch {}
        listeners.forEach((fn) => fn(null, null));
      },
      subscribe(fn) { listeners.add(fn); return () => listeners.delete(fn); },
    };
  }

  const api = { createSettings, DEFS, KEY };
  if (typeof module !== 'undefined' && module.exports) module.exports = api;
  else global.VCSettings = api;
})(typeof window !== 'undefined' ? window : globalThis);
